# ذاكرة المشروع: تطهير البيانات الوهمية وإلغاء البذر الآلي وحوكمة سجل استعلامات الرفوف

**التاريخ:** 26 سبتمبر 2026  
**الفرع الأساسي:** `fix_fake_statistics_data`  
**طلب الدمج المعتمد:** PR #1267  
**الالتزام المدموج إلى main:** `18d5bbe2`  
**النشر الإنتاجي:** تم النشر بنجاح على Hostinger VPS عبر `pnpm prod:deploy` في 268.1 ثانية ومطابقة الفحص الحي على `/healthz` (200 OK)  
**الحالة:** مدموج ومنشور في الإنتاج (Production Live)  

---

## ١. سياق البلاغ والتحقيق الجنائي (RCA)

### بلاغ المالك:
> «الاحصائيات والمعلومات التي تظهر خاطئه وغير حقيقيه ووهميه» (مرفق صورة لشاشة إحصائيات مستفيدي باركود الرفوف تظهر 270 عملية مسح، 102 مستفيداً، وأرقاماً غير واقعية).

### التحقيق الجنائي والأسباب الجذرية:
1. **البذر الآلي عند فراغ الجدول (Auto-Seed on Empty Table):**
   - في PR #1258 (`966ee86b`)، أُضيفت دالة `seedShowroomSampleAnalytics(45)` في `server/services/shelfAnalyticsService.ts` تُنفَّذ تلقائياً بمجرد أن يكون جدول `shelfLookupLogs` فارغاً (`rawTotal === 0`).
   - كان الاستدعاء يقوم بتوليد 45 حركة مسح وهمية بباركودات عشوائية، وأجهزة عشوائية (Android/iOS)، وزوار مصطنعين (`vst_*`)، وتواريخ موزعة عبر الأيام السابقة.
2. **زر البيانات الاسترشادية بالواجهة (Mock Data Trigger):**
   - وُجد زر بالواجهة الأمامية في `ShelfBeneficiariesAnalytics.tsx` باسم «بيانات استرشادية» متصل بإجراء `seedDemo` في الراوتر، وكان يكرر حقن الدفعات عند كل نقرة، ما أدى لتراكم 270 مسحاً وهمياً على قاعدة بيانات الإنتاج.
3. **غياب آلية التصفير والحوكمة الصريحة:**
   - لم تكن هناك وسيلة للإدارة لتصفير السجلات بعد فترات الفحص أو التخلص من البيانات التجريبية، مما رسخ الأرقام الوهمية.

---

## ٢. المعالجات والتحسينات المنجزة

### أ) على مستوى الخادم والخدمات (`server/services/shelfAnalyticsService.ts` و `server/routers/shelfAnalyticsRouter.ts`)
1. **استئصال البذر الآلي نهائياً:**
   - إزالة شرط `if (rawTotal === 0)` وحذف استدعاء `seedShowroomSampleAnalytics(45)`.
   - لا يتم الآن تسجيل أو قراءة سوى حركات الاستعلام الفعلية الصادرة عن مسح الباركود الحقيقي.
2. **استبدال البذر بدالة التصفير الذرية (`purgeShelfLookupLogs`):**
   - حذف دالة البذر القديمة بالكامل، واستبدالها بدالة حوكمة تنفذ `DELETE FROM shelfLookupLogs` مع تسجيل دقيق للعمليات في السجل (`logger`).
3. **حوكمة إجراءات الراوتر:**
   - حذف الإجراء `seedDemo` من `shelfAnalyticsRouter.ts`.
   - إضافة إجراء `purgeLogs` محمي بصلاحيات إدارة الفروع (`storeManagerProcedure`).

### ب) على مستوى الواجهة الأمامية (`client/src/components/shelf-qr/ShelfBeneficiariesAnalytics.tsx`)
1. **إزالة أزرار البيانات الوهمية:**
   - حذف زر «بيانات استرشادية» وكل ما يرتبط به من نصوص وتنبيهات.
2. **إضافة زر تصفير السجل المحمي:**
   - إضافة زر «تصفير السجل» في الشريط العلوي مزوداً بنافذة تأكيد تحذيرية احترافية (`confirm({ variant: "danger" })`) لمنع النقر العرضي.
3. **حالة الفراغ النظيفة والأمينة (Honest Zero State):**
   - عند انعدام العمليات، تظهر شاشة واضحة تنص على «لا توجد عمليات مسح مسجلة بعد» مع زر للانتقال واختبار قارئ الرفوف (`/shelf-lookup`).
   - تصحيح نسبة النجاح لتعرض `0%` بدقة عند انعدام العمليات.

### ج) على مستوى قاعدة البيانات والهجرات (`drizzle/migrations/0368_purge_fake_shelf_lookup_logs.sql`)
1. **الهجرة رقم 0368:**
   - إنشاء الهجرة الموثقة لحذف السجلات الوهمية المحقونة بالبذر التلقائي (`WHERE ipHash IS NULL AND userAgent IS NULL`).
   - تسجيل الهجرة رسمياً في `meta/_journal.json` وحجزها بقفل `coord` الذري.

---

## ٣. نتائج التحقق والاختبارات

1. **حزمة الاختبارات الآلية (`server/services/__tests__/shelfAnalytics.test.ts`):**
   - تحديث الاختبارات للتحقق من الحسابات الواقعية وإلغاء البذر واختبار دالة التصفير `purgeShelfLookupLogs`.
   - نجاح 6/6 اختبارات بنسبة 100% (Duration: 8.02s).
2. **فحص الأنواع (`pnpm check`):**
   - 0 أخطاء (100% Green).
3. **حرّاس الكود القياسية (`pnpm check:guards`):**
   - اجتياز كافة الحراس الـ 45 بنجاح تام (لا إيموجي، لا انحرافات، حوكمة الشاشات والجداول 100%).
4. **فحوصات التكامل المستمر (GitHub Actions CI):**
   - اجتياز كامل لجميع الوظائف الـ 14 عن بُعد (All Green).
5. **الدمج والنشر الإنتاجي المدار (`pnpm prod:deploy`):**
   - دمج PR #1267 في `main` بالالتزام `18d5bbe2`.
   - النشر الذري الناجح على خادم Hostinger VPS (`srv1548487.hstgr.cloud`) في 268.1 ثانية.
   - تطبيق الهجرة 0368 على قاعدة بيانات الإنتاج وتطهير السجلات الوهمية القديمة.
   - ثبات عمال PM2 (`erp-server` و `erp-hr-bridge`) واستجابة فحص الصحة الحي 200 OK على `/healthz`.
