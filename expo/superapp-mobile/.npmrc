virtual-store-dir-max-length=50
# Keep native CMake command wrappers below Windows' legacy process-path limit.
# This changes only pnpm's local link layout; dependency resolution is unchanged.
virtual-store-dir=.pnpm
