# تدقيق جنائيّ عدائيّ — منظومة المرتجعات (١ سبتمبر ٢٠٢٦)

> **البلاغ الحيّ (نصّ المالك عن موظّفيه):** «مرتجع الفواتير وهميّ ولا أثر له ويبتلع المخزن ولا يعكس المخزنية والمالية والنقدية وعند ارجاع منتج لا يزيد المخزن.»

**المنهج:** ستّ عدسات جنائيّة متوازية (مخزون · نقد ودرج · دفتر وذمم · حوكمة وطرق مسدودة · شاشات · قنوات بديلة) ثمّ **محقّق عدائيّ لكلّ عدسة** يحاول دحض كلّ ملاحظة على السطر، ثمّ ناقد اكتمال. ١٤ وكيلاً، **٥٩ ملاحظة: ٥١ صمدت و٨ دُحضت**.

⚠️ هذه الوثيقة مؤرَّخةٌ بلحظة كتابتها. **تحقّق من الشيفرة قبل البناء على أيّ بندٍ فيها.**

---

## ١. الجذر — انحدارٌ من ٣١/٨/٢٦ سُلّم بنصفه

`returns.create` ([`returnRouter.ts`](../server/routers/returnRouter.ts)) لم يعد ينفّذ مرتجعاً: صار يستدعي `requestSalesControl` فيُنشئ صفّاً `PENDING` في `salesControlRequests` ويعود بـ`{requestId, status}`. التنفيذ الفعليّ (`returnSaleInTx`) يقع **فقط** داخل `approveSalesControlRequest`.

الحوكمة نفسها قرارُ مالكٍ سليم. لكنّ الشريحة بُني فيها **مسار الطلب** ولم يُبنَ **مسار اكتشافه** ولا **مسار الخروج منه**، فسقط الطلب في ثقبٍ ثلاثيّ:

| الحلقة | الشيفرة |
|---|---|
| **١) لا يراه أحد** | `superApp.approvalInbox` يجمع خمسة مصادر (inventory/leave/voucher/expense/gift) — ليس فيها `salesControlRequests` · لوحة «طلبات الإرجاع المعلَّقة» في [`Returns.tsx`](../client/src/pages/Returns.tsx) كانت تقرأ **الجدول القديم** `returnRequests` · لا شارة عدد على تبويب «طلبات العمليات» · صفر إشعار |
| **٢) لا يخرج منه أحد** | `assertReviewerSeparation` يحجب الطالبَ **ومنشئَ الفاتورة** على **الاعتماد والرفض معاً**، بلا استثناء أدمن · و`returns.create` محصورٌ بـ`salesManagerProcedure` ⇒ الطالبُ مديرٌ دائماً |
| **٣) والفاتورة تُقفَل معه** | `activeInvoiceId` عمودٌ مولَّد `CASE WHEN status='PENDING' THEN invoiceId` بفهرسٍ فريد **لا يفرّق بين أنواع الطلبات** ⇒ طلبٌ عالق يقفل المرتجع والإلغاء والتصحيح والاستبدال وتعديل الاستحقاق معاً |

**والشاشة تُقسم بعكس ذلك:** حوار التأكيد كان يقول «يستلم الزبون X د.ع نقداً من الدرج، **والبضاعة تعود للرفّ**» وزرُّه «تسجيل المرتجع» — بصيغة الحاضر المؤكَّد. فيفتح الموظّف الدرج ويسلّم النقود ويضع البضاعة على الرفّ، ثمّ يجد سطراً أخضر صغيراً ينفي ذلك.

**الأثر المركَّب:** نقدٌ خرج بلا إيصال ⇒ `computeExpectedCash` لا يعرف به ⇒ **عجزٌ في Z-report يُحمَّل على الكاشير ظلماً** · بضاعةٌ على الرفّ و`branchStock` يقول إنّها بيعت ⇒ «يبتلع المخزن» · لا قيد `RETURN` ⇒ الإيراد وCOGS منفوخان · الفاتورة تبقى `PAID` فتُطالَب ذمّةُ العميل بما رُدّ. **نقضٌ للأركان الخمسة للمبدأ المالي الحاكم دفعةً واحدة.**

> **ومحرّك المرتجع نفسه سليم:** `SIGN.RETURN = +1` و`applyMovement` يزيد الرصيد فعلاً، والبكج يُوسَّع من لقطته، والخدمة تُستثنى بحقّ. العطل كلّه في **الوصول** إليه، لا في حسابه.

---

## ٢. ثقوبٌ مستقلّة عن الحوكمة — تصيب المسارات التي تعمل

| # | الثقب | الحالة |
|---|---|---|
| أ | **`referenceType` مختلف:** `printSaleService` يَسِم حركاته `PRINT_SALE`، و`delivery/returns.ts` يبحث عن `INVOICE` وحده ⇒ إرجاع طرد فاتورة كاشير الطباعة يعكس **الدفتر** ولا يعيد قطعةً إلى **الرفّ** | ✅ أُصلح |
| ب | **السند يُحيي المستند الميت:** `computeInvoiceStatus` تُنتج PENDING/PARTIALLY_PAID/PAID حصراً، ومسارُ الإنقاص (مُستثنى عمداً من حارس المستند الميت) كان يكتبها على فاتورةٍ `RETURNED` فتعود «مدفوعة» — **وتنطفئ معها كلّ حرّاس `isDeadInvoiceStatus`** | ✅ أُصلح |
| ج | **الدرج مُجمَّد في حمولة الطلب:** `refund.shiftId` يُبصَم لحظة الطلب، و`resolveBranchCashShiftTx` يشترط وردية **مفتوحةً الآن** ⇒ كلّ مرتجعٍ نقديّ يُطلَب آخر الدوام ويُعتمَد في اليوم التالي **يفشل حتماً**، ولا حقلَ في شاشة الاعتماد لتبديله | ✅ أُصلح |
| د | **شاشة الاعتماد تتجاهل ما يُدخله المدير:** الجدول يُفتَح فارغاً، والمدير يُدخل كمّيات ويُقسم بها الحوار، بينما `approveRequest` ينفّذ `linesJson` المخزَّنة ⇒ يعتمد «قلمان/١٠٠٠ د.ع» ويُرجع الخادم عشرة | ✅ أُصلح |
| هـ | **مصير البضاعة معروضٌ معكوساً:** الشاشة لا ترسل `restock` للزبون العابر (القرار في `resolution.disposition`) ⇒ `payload.restock === false` يُقيَّم `undefined !== false` ⇒ **«سليمة — تعاد للمخزون» دائماً**، حتى حين اختار الطالب «تالفة» | ✅ أُصلح |
| و | **الطابور القديم يتجاوز حوكمة أمر الشغل:** `createReturnRequest` بلا حارس `sourceType='WORKORDER'` (والمسارات الثلاثة الأخرى ترفضه) ⇒ يُعكَس الإيراد والذمّة بينما `workOrders.status` يبقى DELIVERED، فيُقفَل `reverseDelivery` إلى الأبد وعربونٌ محتجَزٌ بلا مخرج | ✅ أُصلح |
| ز | **قفلٌ متبادل بين الطابورين:** كلٌّ يرفض اعتماد الآخر، وكان `createReturnRequest` بلا الفحص المقابل فيُصنَع القفل عند الإنشاء | ✅ أُصلح |

---

## ٣. قرارا المالك (١/٩/٢٦) وما نُفِّذ بهما

### أ) المرتجع يحتاج شخصين — القرار: **مسارٌ فوريّ للمالك**

`returnSaleAsOwner` ([`returnService.ts`](../server/services/returnService.ts)) ينفّذ مرتجع المالك مباشرةً بلا دورة اعتماد، بثلاثة حرّاس: ① **إعادةُ قراءة `isOwner`/`isActive` داخل المعاملة نفسها** بقفل `.for("share")` — لا تكفي رايةُ الجلسة (نفس اشتراط `assertTreasuryOutException`) · ② **سببٌ إلزاميّ** موثَّقٌ في سجلّ التدقيق (`return.ownerImmediate`) · ③ الأثرُ يمرّ بـ`returnSaleInTx` نفسها بكلّ قيودها وإيصالاتها وحرّاس الدرج والسقوف — **الاختصارُ في الحوكمة لا في المحاسبة**.

و`returns.create` صار يُرجع **نوعاً مُميَّزاً بـ`mode`** (`EXECUTED` / `REQUESTED`) — يمنع بنيوياً اختلاطَ «طلبٌ أُرسل» بـ«مرتجعٌ نُفِّذ» على أيّ مستهلك، والشاشتان (ويب وأندرويد) تتفرّعان عليه.

### ب) أندرويد — القرار: **إصلاحٌ كامل**

- **عقد السلك:** `ReturnCreation` صار `sealed interface` بشقَّين (`Executed`/`Requested`)، والمُحوِّل يقرأ `mode` **ويرمي على المجهول** بدل ابتلاعه. الشكلُ القديم بلا `mode` — الذي كان يُنتج «تم تسجيل المرتجع بقيمة 0» — يسقط الآن صريحاً.
- **الرسائل:** إشعارُ النجاح ونصُّ حوار التأكيد يتفرّعان على `capabilities.returnExecutesImmediately`؛ الطلبُ يُعلَن بصفريّة أثره صراحةً («لا تسلّم الزبون نقوداً ولا تستلم البضاعة»).
- **الحلقةُ المغلقة أُغلقت:** إعادةُ تحميل الفاتورة تخصّ **المنفَّذ الجزئيّ** وحده، فلم يبقَ سببٌ لإعادة الإدخال ولا لارتداد المحاولة الثانية.
- **شاشةُ الاعتماد:** لم تُبنَ شاشةٌ جديدة — بل أُضيف `salesControl` **مصدراً سادساً** لـ`superApp.approvalInbox`/`approvalDetail`، فظهرت طلبات المرتجعات في **صندوق الموافقات القائم على أندرويد وفي `/my-work` معاً** بتغييرٍ خادميٍّ واحد. و`ApprovalKind.SALES_CONTROL` يوجّه الاعتماد إلى `salesControl.approve` والرفض إلى `salesControl.reject` (بسببٍ إلزاميّ).
- الفلترةُ في المصدر الجديد تُطابق `assertReviewerSeparation` حرفياً (تستثني الطالبَ ومنشئَ الفاتورة، **بلا استثناء أدمن**) فلا يُعرَض صفٌّ يُرفض قرارُه؛ و`approvalDetail` يُطبّق الحارس نفسه فلا يصير «عرّافَ معرّفات».
- واختبارا أندرويد اللذان كانا يُغذّيان المحوِّل **الشكلَ القديم يدوياً** (أخضرُ أبداً) صارا يختبران الشكلين الحقيقيَّين + رمي المجهول.

---

## ٤. البنود الأربعة المفتوحة — أُغلقت (١/٩/٢٦)

### أ) رقيبُ الاحتيال كان منزوعَ السلاح — وبفعلٍ ميت

كاشفُ D3-ب («معالجو الإرجاع» — تركّزُ المرتجعات على شخصٍ بعينه) كان يستعلم `auditLogs` بالفعل
`return` + `.create` وهو **فعلٌ لا يكتبه أيّ سطرٍ في الخادم** (الأفعال الحقيقيّة `return.request`
و`return.approveRequest`…) ⇒ **صفرُ صفوفٍ أبداً** على الإنتاج مهما تركّزت المرتجعات.

وأخبثُ ما فيه أنّ اختباره كان **أخضرَ**: يُدرج صفَّ التدقيق **بيده** بنفس النصّ الميت، فيُثبت
أنّ الكاشف يقرأ ما كتبه الاختبارُ نفسه لا ما يكتبه النظام.

**الإغلاق:** [`returns/auditActions.ts`](../server/services/returns/auditActions.ts) — فعلٌ واحد
(`RETURN_EXECUTED_AUDIT_ACTION`) يستورده **الكاتبون الثلاثة** (المالك الفوريّ · اعتماد طلب المحطة ·
الاعتماد المحكوم) **والرقيب** معاً، فإعادةُ تسميته تُحرّك الطرفين أو لا تُصرّف. و`mode` في
`newValue` يُميّز المسار. ويحرسه [`returnAuditContract.test.ts`](../server/services/__tests__/returnAuditContract.test.ts):
يفحص **الشيفرة** لا سلوكاً في قاعدة اختبار، ويمنع عودة النصّ الميت إلى أيّ طرف.

⚠️ وأُضيف `logAudit` للاعتماد المحكوم: التدقيقُ التلقائيّ يكتب `rpc.salesControl.approve` لكلّ
الأنواع معاً (إلغاء/استبدال/استحقاق) فلا يُميّز المرتجع — وتركّزُ المرتجعات لا يُقاس بفعلٍ يخلط
أربع عمليات.

### ب) الأوفلاين صار يقبل مرتجعاً — بحرّاسه

`OfflineOutboxKind` كان `SALE | PRINT_SALE | RECEPTION` بلا مرتجعٍ ولا طابور، فالزبونُ يعود
أثناء الانقطاع ولا مسارَ إلّا الدفعُ من خارج النظام والتسجيلُ لاحقاً — **مصدرُ النقد اليتيم**.

**الإغلاق:** نوع `RETURN` + [`replayReturn.ts`](../server/services/offline/replayReturn.ts)
غلافاً رقيقاً حول `returnSaleInTx` (لا نسخةَ منطقٍ ماليّ ثانية) + `offline.replayReturn` +
التقاطٌ في الشاشة عند **فشل النقل فعلاً** + توجيهٌ في خريطة التفريغ.

| الحارس | لماذا |
|---|---|
| **المالك وحده** (يُقرأ داخل المعاملة بقفل `for("share")`) | التنفيذ الفوريّ سلطتُه؛ والتقاطُ «طلبٍ» يترك النقدَ الخارجَ بلا مستند |
| نافذةُ الالتقاط + نقديّةُ الردّ | نفس `captureWindow.ts` المشترك — لا عتبتان تنجرفان |
| سببٌ إلزاميّ | لا مرتجعَ أوفلاينيّ بلا مستند |
| **السقفُ الماليّ يُقيَّم خادمياً عند الترحيل** | لا يُقيَّم على الجهاز (يحتاج تاريخ القبض كاملاً). رفضُه يُعلّق العنصر في طابور الاسترداد فيصير العجزُ **موثَّقاً بمستند** لا ضياعاً صامتاً |

وهجرة [`0321`](../drizzle/migrations/0321_offline_recovery_return_channel.sql) تُضيف قناة `RETURN`
لطابور الاسترداد: تصنيفُه `RETAIL` كان سيُقرأ بيعاً في طابورٍ عنوانُه «مبيعاتٌ مدفوعة»، والمرتجع
**معاكسُ الاتجاه** — والمدير يقرّر على الاتجاه.

### ج) بديلُ الخزينة كان مبنيّاً وغير موصول

`returnSaleInTx` كان يقفل `DRAWER` **ثابتاً** ويشترط ورديةً مفتوحة، فيُحجَب مرتجعُ الزبون العابر
النقديّ خارج ساعات الوردية حجباً كاملاً — وهو أكثر أنواعه شيوعاً. ونظيرُه `cancelSaleInTx` له
بديلُ TREASURY، و**مفتاحُ المرتجع `SALE_RETURN_COMPENSATION` كان معرَّفاً في السياسة وبلا مستدعٍ واحد**.

**الإغلاق:** توجيهُ مصدر النقد عبر `shiftIdForCashTx`، **بحدودٍ ضيّقة بقصد**: اختيارٌ صريح للدرج ⇒
السلوك القديم حرفياً · ورديّاتٌ مفتوحة موجودة ⇒ السلوك القديم حرفياً · **صفرُ ورديات + فاعلٌ
إداريّ** ⇒ الخزينة (بوسم `assertTreasuryOutException`) · كاشيرٌ بلا وردية ⇒ يبقى مرفوضاً.
والدلوُ يمرّ فعلاً إلى الإيصال والقيد (`TREASURY_CASH` لا `CASH`)، و`TREASURY` لا يدخل
`computeExpectedCash` فتبقى تسويةُ الدرج دقيقة. والشاشةُ **تُصرّح** بالمصدر قبل التأكيد.

### د) قرارُ الرفّ صار سطراً سطراً

الإجبارُ كان على مستوى **الفاتورة** (`sourceType === "WORKORDER" ? false : …`) — صحيحٌ لبند ناتج
أمر الشغل (لم يُخصَم قطّ) لكنّه يبتلع أوّلَ بندٍ مخزنيّ يُضاف إليها.

**الإغلاق:** المعيارُ صار **هل خُصم هذا الصنفُ فعلاً عند البيع؟** (حركةُ OUT على الفاتورة برافديها
`INVOICE` و`PRINT_SALE`) — نفس منطق `delivery/returns.ts`. والتكلفةُ تتبع القرارَ سطرياً
(`restockedPaidCost`/`restockedGiftCost`).

⚠️ **مكافئٌ حرفياً للسلوك القائم** خارج فواتير WORKORDER — والنطاقُ محصورٌ بها بقصد، كي لا يتحوّل
«غيابُ حركةٍ تاريخيّة» في فاتورةٍ قديمة إلى منعِ إرجاعٍ صامت. يحرسه اختبارٌ صريح.

⚠️ **وانحدارٌ أمسكه الاختبار أثناء العمل:** أوّلُ صياغةٍ حصرت حصّةَ الأمانة بالمُعاد للرفّ فكسرت
**عكسَ التزام المودِع** الذي يجب أن يقع **دائماً** (قرار المالك ش٣). الاستعمالان مفصولان الآن:
`byConsignor` (AP) على كلّ المُرجَع، وحصّةُ خصمِ COGS على المُعاد وحده.

## ٥. الأخضر الكاذب في الاختبارات

- **٣٣ ملفَّ اختبارٍ تمسّ `returnSale`/`returnSaleInTx`؛ ٣١ منها بصفر مرجعٍ للراوتر.** ولا اختبارَ واحدٌ في المستودع يستدعي `returns.create` على قاعدةٍ حقيقية.
- الاختبار الوحيد على `returns.create` (`returnRouterWalkIn.test.ts`) **يُموّه الحوكمة كلّها** بـ`vi.mock` على `controlRequests`.
- `salesControlRequests.test.ts` يُهيّئ **مديرَين مستقلَّين** ⇒ يجد مُعتمِداً دائماً؛ عالمٌ لا يشبه فرعاً بمديرٍ واحد. **هذا سبب أنّ الحزمة كانت خضراء والبلاغ حقيقيّ.**
- ⚠️ `salesControlApprovals.test.tsx` يؤكّد أنّ `returnRouter` **لا** يستدعي `returnSale` — حارسٌ يُثبّت الحوكمة، فأيّ إصلاحٍ يعيد التنفيذ إلى الراوتر يُحمّره. مقصودٌ الآن، لكن اعرفه قبل أن تُفاجأ به.

---

## ٦. ادّعاءاتٌ توثيقيّة خالفت الشيفرة (صُحّحت أو تلزم تصحيحاً)

| المكان | الادّعاء | الواقع |
|---|---|---|
| `docs/campaign-log.md` | «المرتجعات (إرجاع من فاتورة + إعادة للمخزون + استرداد)» ضمن «١٠٠٪ خلفية+واجهة+تحقّق» | صفر أثر |
| `docs/cash-core-design.md` | يجدول `returnService.createReturn` | دالّة غير موجودة |
| `docs/consignment-design-2026-07-20.md` | «شاشة مرتجع البيع بلا أيّ تغيير على الكاشير» | الشاشة لا تنفّذ شيئاً |
| `CLAUDE.md` | «`returns.create` يشترط بنداً» | الشرط في الخدمة وحدها؛ `requestSalesControl` **لا يتحقّق من حمولة المرتجع إطلاقاً** |

---

## ٧. ما نُفِّذ في هذه الشريحة

| الإصلاح | الملف |
|---|---|
| حوار التأكيد يقول الحقيقة (يتفرّع على وضع الطلب/الاعتماد) + بطاقة الطلب المعلّق + حجب الإرسال المكرّر + قفل كمّيات الاعتماد | [`ReturnComposer.tsx`](../client/src/components/returns/ReturnComposer.tsx) |
| `getInvoice.pendingRequest` (+`canReviewIt` مشتقّةٌ خادمياً) و`getRequest` لبنود الطلب | [`returnRouter.ts`](../server/routers/returnRouter.ts) |
| `cashRouting` — توجيهُ الدرج والمرجع لحظة الاعتماد بلا مسّ المبلغ أو الطريقة · و`withdrawSalesControlRequest` | [`sale/controlRequests.ts`](../server/services/sale/controlRequests.ts) |
| منتقي الدرج + زرّ «سحب الطلب» + تصحيح عرض مصير البضاعة | [`SalesControlApprovals.tsx`](../client/src/pages/SalesControlApprovals.tsx) |
| المستند الميت يبقى ميتاً | [`voucher/invoiceAllocation.ts`](../server/services/voucher/invoiceAllocation.ts) |
| إرجاع الطرد يعيد حركات `PRINT_SALE` أيضاً | [`delivery/returns.ts`](../server/services/delivery/returns.ts) |
| حارس WORKORDER + الفحص المتقابل للطابورين | [`returns/requests.ts`](../server/services/returns/requests.ts) |
| لوحة الطلبات المعلّقة تجمع الطابورين + بطاقة خطأٍ صريحة بدل إخفاءٍ صامت | [`Returns.tsx`](../client/src/pages/Returns.tsx) · [`SalesReturns.tsx`](../client/src/pages/SalesReturns.tsx) |
| هجرة `0319` — حالة `WITHDRAWN` + توسيع CHECKين (مُجرَّبة بـ`migration-dry-run` على مخطّطٍ إنتاجيّ فعليّ) | [`0319_…sql`](../drizzle/migrations/0319_returns_withdraw_and_owner_override.sql) |
| مسارُ المالك الفوريّ (`returnSaleAsOwner`) + عائدٌ مُميَّزٌ بـ`mode` | [`returnService.ts`](../server/services/returnService.ts) · [`returnRouter.ts`](../server/routers/returnRouter.ts) |
| `salesControl` مصدراً سادساً لصندوق الموافقات (ويب `/my-work` + أندرويد) | [`superAppRouter.ts`](../server/routers/superAppRouter.ts) |
| عقدُ سلك أندرويد + رسائلُه + `ApprovalKind.SALES_CONTROL` | `android-native/…/SalesMappers.kt` · `SalesState.kt` · `SalesScreen.kt` · `ApprovalsRepository.kt` |
| فعلُ تدقيقٍ واحدٌ للكاتبين والرقيب + عقدٌ نصّيّ يحرس الانحراف | [`returns/auditActions.ts`](../server/services/returns/auditActions.ts) · [`returnAuditContract.test.ts`](../server/services/__tests__/returnAuditContract.test.ts) |
| المرتجع الأوفلاينيّ: نوعٌ + ترحيلٌ + التقاطٌ عند فشل النقل + قناة استرداد (هجرة 0321) | [`replayReturn.ts`](../server/services/offline/replayReturn.ts) · [`offlineReturnReplay.test.ts`](../server/services/__tests__/offlineReturnReplay.test.ts) |
| توجيهُ مصدر النقد (درج ⇒ خزينة للإداريّ) بوسم `SALE_RETURN_COMPENSATION` | [`returnService.ts`](../server/services/returnService.ts) |
| قرارُ الرفّ سطراً سطراً + تكلفةٌ تتبعه | [`returnService.ts`](../server/services/returnService.ts) |
| حزمةُ إعادة إنتاج البلاغ ⇒ صارت حارساً على الإصلاح | [`returnsForensicDeadlock.test.ts`](../server/services/__tests__/returnsForensicDeadlock.test.ts) |

### مبدأ السحب — لماذا لا يخرق فصل المهام
Maker-Checker يحرس **حركة المال**. والسحبُ لا يُحرّك ديناراً ولا قطعةً: أثرُه الوحيد إخراجُ `status` من `PENDING` فيصير `activeInvoiceId` بـ`NULL` وتتحرّر الفاتورة. وهو **تراجعُ صاحب الاقتراح عن اقتراحه**، لا مراجعةٌ له. **الاعتماد يبقى محكوماً بمراجعٍ مستقلٍّ بلا تغييرِ حرف.** والشيفرة كانت قد أدركت هذا في الطابور القديم ثمّ نقضته: تعليق `requests.ts` يقول «إذا أصبح الطلب stale فرفضه هو المخرج التشغيليّ الصحيح» ثمّ يُبطله `assertReviewerAuthority`.

⚠️ **تغييرُ سلوكٍ مقصود:** طلبُ الإرجاع من طابور المحطة على فاتورة `sourceType='WORKORDER'` يُرفَض الآن ويُوجَّه إلى شاشة عكس تسليم أمر الشغل. الفواتير الأخرى في الطابور (منشأُها POS) لا تتأثّر.

---

## ٨. المتابعة

- **إصدار أندرويد:** إصلاحُ عقد السلك والرسائل وصندوق الموافقات في الشيفرة ومُختبَر، ولا يبلغ
  المختبِرين قبل رفع `versionCode` في مواضعه المتزامنة وبناءِ AAB موقَّع ورفعٍ يدويّ إلى Play.
  قرارُ الإصدار للمالك.
- **هجرتان للنشر:** `0319` (حالة `WITHDRAWN`) و`0321` (قناة استرداد `RETURN`) — كلتاهما مُجرَّبة
  بـ`migration-dry-run` على مخطّطٍ أُعيد فعلاً إلى حالة الإنتاج (الـenum والقيود القديمة)، لا على
  مخطّطٍ يحمل الصيغة الجديدة سلفاً.
- **حافّةٌ باقيةٌ مقصودة:** المرتجع الأوفلاينيّ محصورٌ بحساب المالك. لغيره يبقى المرتجعُ أونلاين
  بمسار الطلب والاعتماد — لأنّ التقاطَ «طلبٍ» أوفلاينياً يترك النقدَ الخارجَ بلا مستند، وهو عينُ
  العطب الذي أُغلق.
- **تحسينٌ لم يُنفَّذ (ليس ثقباً):** لا شارةَ عدد على تبويب «طلبات العمليات» (`HubTab` بلا حقل
  badge) ولا إشعارَ عند إنشاء الطلب أو حسمه. صندوقُ الموافقات صار يعرضها (§٣-ب) فلم تبقَ
  غيرَ مرئيّة، لكنّ الشارةَ والإشعار يبقيان تحسيناً مفتوحاً.
