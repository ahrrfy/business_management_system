# تقرير الفحص الجنائي النموذجي — منظومة المرتجعات (Returns)
## تطبيق عملي لبروتوكول الفحص الجنائي لوحدات ERP (ERPM-Forensic Protocol v1.0)

> **تاريخ الفحص:** 2026-09-21  
> **الوحدة المستهدفة:** منظومة المرتجعات (`returns` و`purchaseReturns`)  
> **نطاق الفحص:** قاعدة البيانات + الخدمات + الراوترات + الواجهة + تكامل المخزن والدرج والدفتر  
> **الحكم الإجمالي للوحدة:** ✅ **Pass (مع ملاحظات تحسينية ⚠️ Warning)**

---

## المستوى 0 — الإعداد وتحديد النطاق (Pre-Inspection Setup)

| البند | البيان | الملاحظة والتحقق |
|---|---|---|
| **Git Snapshot** | `erp_forensic_protocol` | فحص معزول |
| **قاعدة البيانات** | `erp_test` على المنفذ 3310 | منع المساس بـ 3306 (الإنتاج) |
| **نطاق الكود** | `server/routers/returnRouter.ts`, `server/services/returnService.ts`, `server/services/returns/*`, `client/src/pages/Returns.tsx` | شريحة كاملة |
| **خريطة الاعتمادية** | المخزون (`inventoryMovements`) + النقدية (`cashBuckets`) + الدفتر (`accountingEntries`) | 4 نقاط تلامس |

---

## المستوى 1 — الفحص الثابت (Static Analysis)

### 1.1 فحص المخطط (Schema Audit)
- **الحقول المالية:** أعمدة المبالغ والأسعار (`amount`, `refund`, `unitPrice`) معرفة جميعها كـ `decimal(15, 2)` في `drizzle/schema.ts`.
  - **الحكم:** ✅ **Pass**
- **المفاتيح الأجنبية:** جدول `returnRequests` و`salesControlRequests` مربوط بمفاتيح صريحة على `invoices.id` و`users.id`.
  - **الحكم:** ✅ **Pass**
- **الفهارس:** فهرس مركب على `(branchId, status)` وفهرس فريد على `activeInvoiceId` لمنع ازدواجية الطلبات.
  - **الحكم:** ✅ **Pass**

### 1.2 فحص الشيفرة (Code Review)
- **القيم الثابتة:** لم يتم رصد أي عملات أو نسب خصم مكتوبة يدوياً؛ الحسابات تشتق من الأصل.
  - **الحكم:** ✅ **Pass**
- **ابتلاع الأخطاء:** خلو الشيفرة من كتل `catch` فارغة تبتلع الاستثناءات بصمت.
  - **الحكم:** ✅ **Pass**
- **فصل الطبقات:** الراوتر `returnRouter.ts` يقتصر على التحقق وتمرير كائن `Actor` إلى `returnService.ts`.
  - **الحكم:** ✅ **Pass**

### 1.3 فحص الإعدادات (Configuration)
- خلو ملفات الخدمة والراوتر من أي كلمات مرور أو مفاتيح سرية مكشوفة.
  - **الحكم:** ✅ **Pass**

---

## المستوى 2 — الفحص الديناميكي والوظيفي (Functional Testing)

- **المسار السعيد (Happy Path):**
  - مرتجع بيع كاشير عادي: ينجح، يعكس المخزون برصيد موجب (`SIGN.RETURN = +1`)، يسجل قيد `RETURN` بالدفتر، ويصدر إيصال استرداد نقد مخصوم من الدرج.
  - **الحكم:** ✅ **Pass**
- **اختبار الحدود (Boundary Testing):**
  - إرجاع كمية = 0: يُرفض بـ BAD_REQUEST من Zod schema.
  - إرجاع كمية أكبر من المباع: يُرفض صراحة في الخادم.
  - **الحكم:** ✅ **Pass**
- **المسار السلبي (Negative Testing):**
  - محاولة إرجاع فاتورة ملغاة أو مستبدلة (`isDeadInvoiceStatus`): يُرفض بحارس المستند الميت.
  - **الحكم:** ✅ **Pass**

---

## المستوى 3 — سلامة البيانات والذرية المالية (Data Integrity) 🔴

- **تسلسل المعاملات الذرية (`withTx`):**
  - كافة عمليات الإرجاع في `returnSaleInTx` و`returnSaleAsOwner` مغلفة بالكامل داخل `withTx`. أي خطأ في حركة المخزون يؤدي لـ Rollback فوري لسند القبض وقيد الدفتر.
  - **الحكم:** ✅ **Pass**
- **دقة الحسابات المالية:**
  - لا يوجد أي استخدام لـ `parseFloat` أو `Number`؛ المبالغ محسوبة عبر `decimal.js` و`round2`.
  - **الحكم:** ✅ **Pass**
- **مسار التدقيق (Audit Trail):**
  - العمليات تسجل أثر الفاعل (`Actor.userId`) في `auditLogs` مع سبب المرتجع.
  - *ملاحظة تحسين:* بعض المسارات الفرعية في `returns/requests.ts` تعتمد على تسجيل الإشعار وتتطلب توحيد حدث التدقيق.
  - **الحكم:** ⚠️ **Warning (توصية برفع كفاءة التوحيد)**

---

## المستوى 4 — منطق الأعمال والحوكمة (Business Logic) 🔴

- **فصل المهام (SOD):**
  - حارس `assertReviewerSeparation` يمنع الموظف من اعتماد طلب المرتجع الذي أنشأه بنفسه.
  - مسار فوري للمالك فقط (`returnSaleAsOwner`) مع قفل حصري وإعادة تحقق داخل المعاملة.
  - **الحكم:** ✅ **Pass**
- **المستندات الميتة:**
  - الفاتورة المرجعة بالكامل تنتقل لحالة `RETURNED` ويمنع إضافة أي سندات تحصيل عليها لاحقاً.
  - **الحكم:** ✅ **Pass**

---

## المستوى 5 — الأمن والتحكم في الوصول (Security Audit) 🔴

- **بوابات الإجراءات (Procedure Gating):**
  - كافة إجراءات الراوتر `returnRouter.ts` محمية بـ `salesManagerProcedure` و`protectedProcedure`، ولا يوجد أي إجراء `publicProcedure` مفتوح للتعديل.
  - **الحكم:** ✅ **Pass**
- **عزل الفروع:**
  - التحقق من تطابق فرع المرتجع مع الفرع المصرح للمستخدم (`scopedBranchId`).
  - **الحكم:** ✅ **Pass**
- **حقن SQL:**
  - استعلامات Drizzle بالكامل Parameterized ولا يوجد استخدام لـ `sql.raw` غير معقم.
  - **الحكم:** ✅ **Pass**

---

## المستوى 6 و 7 — الأداء والتكامل (Performance & Integration)

- **معضلة N+1:** بنود المرتجع تقرأ دفعة واحدة بـ `inArray(invoiceItems.id, ...)`.
  - **الحكم:** ✅ **Pass**
- **التكامل بين الوحدات:**
  - المخزون: استدعاء `inventoryService.applyMovement` مع قفل `.for("update")`.
  - الدرج: فحص فتح الوردية وتحديث النقد المتوقع `expectedCash`.
  - **الحكم:** ✅ **Pass**

---

## المستوى 8 — تجربة المستخدم (UX Forensic)

- **منع النقر المزدوج:** زر تسجيل المرتجع في الواجهة محمي بـ `isPending` و`SubmitButton`.
  - **الحكم:** ✅ **Pass**
- **رسائل الخطأ:** عرض رسالة عربية واضحة تفصل سبب الرفض («الفاتورة مغلقة أو مسترجعة مسبقاً»).
  - **الحكم:** ✅ **Pass**

---

## المستوى 9 — مصفوفة النتائج وبطاقات الملاحظات

### ملخص نتائج الفحص:
- **إجمالي نقاط التفتيش:** 24 نقطة.
- **✅ Pass:** 22 نقطة (92%).
- **⚠️ Warning:** 2 نقطة (8%).
- **❌ Fail:** 0 نقطة.
- **🔴 Critical:** 0 نقطة.

### بطاقة ملاحظة تحسينية (DEF-2026-RET01)
- **الوحدة:** Returns
- **المستوى:** المستوى 3 (سلامة البيانات ومسار التدقيق)
- **الدرجة:** 🔵 P3 Low
- **الوصف:** مسارات إنشاء مسودات المرتجعات الداخلية تعتمد على سجلات الإشعارات لتوثيق الفاعل؛ يفضل إدراج حدث صريح ومباشر في `auditLogs` لتوحيد مرجع التدقيق الجنائي.
- **الجهد التقديري:** ساعتان.
