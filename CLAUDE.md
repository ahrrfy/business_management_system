# CLAUDE.md — نظام إدارة أعمال الرؤية العربية

> هذه الوثيقة هي **مصدر الحقيقة** للمشروع. تُحمَّل تلقائياً لكل جلسة/وكيل. اقرأها أولاً قبل أي عمل — تُغني عن إعادة اكتشاف السياق (توفير ميزانية).

## ١. هوية المشروع
- الشركة: **الرؤية العربية للتجارة العامة — المكتبة العربية للطباعة والقرطاسية**.
- السوق: **العراق** (عربي RTL، عملة دينار عراقي IQD، **بلا ضريبة VAT** افتراضياً = معدّل 0%).
- النشاط: مطبعة + مكتبة قرطاسية، **فرعان** (رئيسي MAIN كل الخدمات + فرع مبيعات SALES). قطاعات: طباعة، قرطاسية (مفرد/جملة)، هدايا/تخرج، تجهيزات مكتبية، عملاء أفراد/تجار/شركات/دوائر حكومية.
- v2 **إعادة تأسيس نظيفة** بعد مشروع Manus (الإرث مؤرشف في `_legacy/`، مُستثنى من git).

## ٢. التقنية والبنية
- React 19 + Vite + Tailwind v4 + shadcn/ui + wouter + TanStack Query؛ tRPC v11؛ Express؛ Drizzle ORM (mysql2) + MySQL 8؛ decimal.js؛ TypeScript.
- مصادقة محلية مملوكة (JWT عبر jose + scrypt) — **لا OAuth ولا Manus**.
- **مسار الطلب — اقرأه مرّةً، يُغنيك عن تتبّع عشرات الملفات:**

```
client/src/pages (٢٢٥ صفحة، wouter + TanStack Query)
   └─ client/src/lib/trpc.ts ──POST /api/trpc──▶ server/routers.ts (برميل يجمع ٨٢ راوتراً)
        └─ server/routers/*  : تحقّق zod + اختيار الإجراء المُبوَّب (بلا منطق أعمال)
             └─ server/services/*  : منطق الأعمال داخل withTx (١٦٩ ملفاً/حزمة)
                  └─ drizzle/schema.ts (٢١٠ جداول، ١٠٨٧٩ سطراً) ──▶ MySQL 8
```

- **قاعدة الطبقات:** الراوتر بلا منطق أعمال، والخدمة **لا تقرأ `ctx`** — تستقبل `Actor { userId, branchId, role }` (`server/services/tx.ts`). كسرُ هذا يُعمي حرّاس الصلاحيات ويجعل الخدمة غير قابلة لإعادة الاستعمال من قناةٍ أخرى (أوفلاين/أندرويد/استيراد).
- **مفردات التفويض = `server/trpc.ts`** (٨٠ إجراءً مُصدَّراً جاهزاً): **لا تكتب فحص صلاحيةٍ جديداً — اختر الإجراء المناسب.** الاصطلاح `query ⇒ READ` و`mutation ⇒ FULL`؛ `moduleProcedure(roles, moduleKey, level)` = بوّابة وحدة + إلزام فرعٍ مُسنَد؛ و`branchScopedProcedure` يحقن `scopedBranchId` (عزل الفرع) و`scopedOwnerId` (عزل سجلّات الموظف: يرى ما أنشأه فقط). الخريطة المشتركة مع الواجهة في `shared/permissions.ts` — والإنفاذ النهائيّ **خادميّ دائماً**.
- **المسارات المختصرة:** `@/*` ⇐ `client/src/*` و`@shared/*` ⇐ `shared/*` (مضبوطة في `tsconfig.json` + `vite.config.ts` + كلا ملفَّي vitest).

## ٣. التشغيل (Windows + PowerShell)
```
docker start erp-mysql            # حاوية MySQL (mysql:8, root/erp_root_pw, منفذ 3306, قاعدة erp)
pnpm install
pnpm db:push                      # هجرة المخطط (محروسة بـ scripts/db-push-guard.mjs)
CONFIRM_SAMPLE_DATA_SEED=1 pnpm seed   # admin + فرعان + منتجات عيّنة (idempotent) — التأكيد إلزاميّ
pnpm dev                          # http://localhost:3000
pnpm check                        # فحص الأنواع (tsc --noEmit)
pnpm check:guards                 # حزمة الحرّاس الأحد عشر — بوّابة ما قبل الدفع (§٣.١)
pnpm test:db:init                 # تهيئة قاعدة اختبار هذه الشجرة طازجةً (بعد تغيّر المخطط)
pnpm test                         # الحزمة الكاملة (vitest، TZ=UTC، ٥٦٧ ملف اختبار)
pnpm test:unit                    # ١٠٠ ملف منطقيّ بلا قاعدة — ثوانٍ، للتكرار السريع
pnpm build && pnpm start          # بناء الإنتاج (vite + esbuild) ثم تشغيله من dist/
pnpm format                       # prettier
```

**تشغيل اختبارٍ واحد** (الحزمة الكاملة ٢٠-٤٥ دقيقة — لا تشغّلها للتحقّق من تغييرٍ صغير):
```
pnpm exec cross-env TZ=UTC vitest run server/services/__tests__/sale.test.ts
pnpm exec cross-env TZ=UTC vitest run server/services/__tests__/sale.test.ts -t "اسم الحالة"
```
- **`pnpm test:unit` بقائمة سماحٍ صريحة** (`vitest.unit.config.ts`) — الاختبار المنطقيّ الجديد **لا يُشغَّل حتى يُسجَّل فيها**، واستدعاؤه بلا `--config vitest.unit.config.ts` يجرّ تهيئةَ القاعدة فيسقط بـ`ECONNREFUSED 3310`.
- **`TZ=UTC` إلزاميّ:** سكربت `test` يضبطها بـ`cross-env`، والاستدعاء المباشر لا ⇒ تُحمَّر اختبارات التاريخ زوراً.
- **قاعدة الاختبار — مساران مختلفان، اعرف أيّهما أنت فيه** (`__setup__.ts` يمسح **كل صفوف كل الجداول** بعد كل ملف ⇒ الخلط بينهما يُسقِط اختبارات جلسةٍ أخرى):
  - **بلا `TEST_DATABASE_URL`** (شجرة عملٍ عادية/harness): تُشتقّ تلقائياً من اسم المجلّد — `erp_<اسم-المجلّد>_test` على **`127.0.0.1:3310`** (`scripts/lib/test-db-name.mjs`)، والمستودع الأصل وحده يبقى `erp_test`.
  - **⚠️ بعد `pnpm session:new`**: السكربت **يكتب `TEST_DATABASE_URL` صراحةً** مشتقّاً من `DATABASE_URL` في `.env` الجذر ([session.mjs:177-182](scripts/session.mjs#L177)) ⇒ يرث **منفذ 3306** بالإعداد الافتراضي، فيُلغي الاشتقاق أعلاه. وحارسا 3306 يرفضانه عندئذٍ: [`init-test-db.mjs:48`](scripts/init-test-db.mjs#L48) و[`__setup__.ts:69`](server/services/__tests__/__setup__.ts#L69). للتشغيل: إمّا **عدِّل `TEST_DATABASE_URL` في `.env` الجلسة إلى `mysql://root:testpw@127.0.0.1:3310/erp_<الجلسة>_test`** (المفضَّل)، أو اقبل قاعدة الجلسة على 3306 صراحةً بـ`ALLOW_PORT_3306=1` و`ALLOW_TEST_DB_PORT_3306=1` — **ولا تفعل الثانية إن كانت حاوية 3306 لديك مرآةَ الإنتاج**.
  - **الأصحّ بنيوياً** أن يُولّد `session:new` رابط الاختبار من قاعدة 3310 لا من `DATABASE_URL` — إصلاحٌ مفتوح (أمسكته مراجعة Codex على PR #600).
- `fileParallelism: false` — الملفات تتسلسل؛ لا تسريع بالتوازي. **وCI يجزّئها على شاردتين** بقاعدتين معزولتين (`test-shard-1-of-2`/`2-of-2`، واتحادُهما الحزمةُ كاملةً بلا تداخل)؛ ⚠️ **pnpm يبتلع `--shard` بالصيغتين** (`pnpm test --shard=…` و`pnpm run test -- --shard=…`) ⇒ الاستدعاء `pnpm exec cross-env TZ=UTC vitest run --shard=…` مباشرةً، ويحرسه فحصُ عدد الملفات المُشغَّلة في نفس الخطوة بعد أن كانت التجزئة **خاملة** تُعيد الحزمة كاملةً على كل شاردة (#637).
- ⚠️ **`tsconfig.json` يستثني `**/*.test.ts`** ⇒ `pnpm check` **لا** يفحص أنواع الاختبارات؛ خطأ النوع فيها يظهر عند `vitest` وحده.
- **`pre-commit` = `pnpm check && pnpm check:guards`** (~دقيقة) لا الحزمة الكاملة — أُعيدت معايرته لأنّ الحارس البطيء كان يُتجاوَز بـ`--no-verify` فيصير مسرحياً. الحزمة الكاملة مكانها CI (`check-test-build` فحصٌ مطلوبٌ لحماية `main`).
- **البذرة محروسة بسياسةٍ صريحة** ([`server/config/seedPolicy.ts`](server/config/seedPolicy.ts)) — أمسكها التحقّق الحيّ ١٦/٨، وكانت تُعطّل أيّ بيئةٍ جديدة:
  - `pnpm seed` وحده **يفشل**: يلزمه `CONFIRM_SAMPLE_DATA_SEED=1` (بيانات عيّنة + حسابات). وللإنتاج `pnpm seed:prod`.
  - **`Admin@12345` مرفوضة صراحةً** (ضمن `PUBLISHED_PASSWORDS`)، وكذلك أيّ كلمةٍ أقصر من ١٠ خانات أو لا تحقّق `isStrongPassword` ⇒ **بيانات الدخول القديمة الموثَّقة هنا لم تعد تُنشئ حساباً**. اضبط `ADMIN_PASSWORD` قويةً في `.env` قبل البذر، وادخل بها.
  - ⚠️ **فخّ dotenv:** القيمة غير المُقتبَسة تُقتطع عند أوّل `#` (`ADMIN_PASSWORD=Aa#Bb` تصير `Aa`) فتسقط البذرة برسالة «كلمة ضعيفة» مُضلِّلة — **اقتبس أيّ قيمةٍ فيها `#`**.
- دخول: `admin@alroya.local` + `ADMIN_PASSWORD` التي ضبطتَها. الإعدادات في `.env` (مُستثنى من git).
- **علل بيئية محسومة:** `tsx watch` معطوب على هذا الجهاز → أمر `dev` يستخدم `node --watch-path=./server ... --import tsx`. Docker Desktop ينهار بعلّة «Inference manager» → عند الحاجة: أوقف كل عمليات docker ثم أعد التشغيل. الكوكي `sameSite:"lax"` ليعمل على localhost.
- **قاعدة الاختبار المحلّية (erp-test-db@3310، MySQL 8.4.9):** `pnpm test`/`pre-commit` يفترضان مخطّطاً مُهيَّأً بالحاليّ؛ لكن `db:push` **التزايديّ** على قاعدة منجرفة يفشل على 8.4 (`ER_DROP_INDEX_FK`) فتتجمّد القاعدة وتسقط الاختبارات بـ«Unknown column» لا علاقة لها بالكود. الحلّ: **`pnpm test:db:init`** (إسقاط+إنشاء طازج ثم `db:push` + `ci-apply-extra-migrations` — نفس مسار CI) بدل الـpush التزايديّ. حرّاسه ترفض 3306 (مرآة الإنتاج) وأيّ اسم بلا «test». التفصيل والجذر (اسم FK تلقائيّ >٦٤ محرفاً كان يُفشل `db:push` الطازج على 8.4): [`docs/local-test-db.md`](docs/local-test-db.md).

## ٣.١ الحرّاس الآليّون (ratchets) — تُفشِل CI فوراً
كلٌّ سكربت `node` مستقلّ في `scripts/`، ويُجمَّعون بـ`pnpm check:guards`:

| الأمر | يمنع |
|---|---|
| `check:emoji` | أيّ إيموجي في `client/**` (البديل: أيقونة `lucide-react`) |
| `check:money-schemas` | `z.string()` عاريةً على حقلٍ ماليّ (استعمل `positiveMoneyString`) |
| `check:form-inputs` | مدخلات نماذج خارج المكوّنات الموحّدة (`MoneyInput`/`AppSelect`) |
| `check:branch` | `?? 1` — الفرع الافتراضيّ الصامت (بابُ IDOR تاريخيّ). يمسح `server/**` و**`client/**`** معاً بخطّ أساسٍ مجمَّد ⇒ لا يُضاف افتراضٌ صامتٌ جديد في شاشة |
| `check:migrations` | ترقيم/سجلّ الهجرات المكسور (`_journal.json`) — **ويكشف تصادم الرقم مع `origin/main` قبل الدفع** (§٧ ملفٌ ساخن)؛ يتخطّى بصمتٍ حين لا يكون `origin/main` محلياً ⇒ **`git fetch origin main` أوّلاً وإلّا كان الحارس أعمى** |
| `check:orphans` | إجراءً خادمياً بلا مستدعٍ في الواجهة (بوّابة DoD §٤) |
| `check:date-boundaries` | حدود يومٍ محلّية بدل `businessDay` (UTC) |
| `check:colors` | لون حالة/مالٍ خامّاً يتجاوز التوكنز |
| `check:authz` | انحراف جرد الصلاحيات عن خطّ الأساس |
| `check:nginx-abuse` | انحراف إعداد nginx عن عقد الحماية |
| `check:compose` | تعذّر تحليل `docker-compose.yml` بقيم اختبار آمنة |

- CI يشغّل هذه الأحد عشر **عدا `check:authz`**؛ يحرسها بدلاً منه وظيفةٌ منفصلة `authz-guard` عبر `authz-guard-diff.mjs` (فرقُ قاعدة الدمج: ترفض ما **يُدخِله الـPR** من `raw-role` أو إجراءٍ بلا بوّابة وحدة، ولا تُحاسب على انحراف `main` القائم). ويشغّل CI أيضاً `check:mobile-release` و`db:verify` و`verify-digital-cards-integrity --selftest`.
- ⇒ **`pnpm check:guards` محلياً أوسع من خطوات CI الفردية** — شغّله قبل الدفع.

## ٤. القاعدة الحاكمة (Definition of Done) — لا تُكسَر
> **اكتمال أي وحدة = خلفية (API) + واجهة (شاشة) + فحص (`pnpm check`) + تحقّق (اختبار/جولة بصرية) = ١٠٠٪.**
> الخلفية وحدها بلا شاشة = **وهم اكتمال**. تُبنى كل وحدة **شريحة رأسية كاملة** (زر في الواجهة → خدمة → معاملة ذرية → DB → عرض)، تُتحقَّق بصرياً، وتظهر في **قائمة التنقّل** — ثم ننتقل للتالية. الترتيب: الكاشير أولاً، ثم وحدة-وحدة.

**⛔ بوّابة بدء الجلسة الإلزامية (قبل أي كتابة كود — لا استثناء):** «نفّذ فوراً» أدناه **مشروطٌ** باجتياز هذه البوّابة أولاً. قبل اختيار/بدء أي وحدة:
> ١) **افحص الجلسات المتزامنة:** اسرد جلسات المشروع النشطة (`mcp__ccd_session_mgmt__list_sessions`) + `pnpm coord:list` (الصورة الحيّة للشرائح المُدّعاة)؛ أي جلسة `isRunning` في نفس المجلد = مالكة محتملة — لا تلمس شريحتها.
> ٢) **افحص العمل غير الملتزم:** `git status`؛ أي ملف معدّل/غير متعقَّب = شريحة جلسة أخرى جارية. **ممنوع** البدء بوحدة تتقاطع معه (افحص بدلالة `git diff` لا التخمين).
> ٣) **اعزل جلستك ثم ادّعِ شريحتك:** `pnpm session:new <موضوع>` (ينشئ فرع `session/<موضوع>` + قاعدة/منفذ/‎.env مستقلّين + يسجّلك)، ثم `pnpm coord:claim <موضوع> --files <ملفاتك>` **قبل أي كتابة**. الكتابة على `main` محظورة آلياً بـ hook. إن وجدت WIP لغيرك ⇒ **توقّف وأبلغ** — لا تطمس عمله.
> ٤) عند اكتشاف تقاطع لحظي أثناء العمل (ملف «تغيّر منذ القراءة»، أو ظهور ملفات/تجهيز فهرس لم تصنعه): **توقّف، لا تطمس، أبلغ المالك** — التزامن قرار تنسيقٍ للمالك لا قرارٌ تقنيٌّ تتخذه وحدك.
>
> **سبب القاعدة (٦ يونيو ٢٠٢٦):** ٣ جلسات تسابقت على `main` على شريحة المشتريات نفسها فتضاربت وتكرّر العمل. الفحص المسبق + العزل يمنعان ذلك حتماً.
>
> **🔒 الإنفاذ الآلي (coord) — لم يعد البروتوكول طوعياً:** `scripts/coord-core.mjs` + hooks في `.claude/settings.json` يُنفّذان البوّابة ميكانيكياً: **قفل ذرّي للشريحة** (`O_EXCL` ⇒ سباق مستحيل)، و**رفض الكتابة** على `main`، أو على ملف تملكه جلسة أخرى حيّة، أو على ملف ساخن لغير قائد الدمج (`_integration`). أوامر: `pnpm coord:list|status|claim|release|reclaim`. مخرج طوارئ المالك: `COORD_BYPASS=1`. التفصيل في `docs/playbook.md`.

**أسلوب العمل (تفضيل دائم — مُلزِم):** تصرّف باستقلالية كموظف محترف حريص وأمين ودقيق: **بعد اجتياز بوّابة بدء الجلسة، اختر الخطوة التالية الأصحّ ونفّذها فوراً** بالمنهجية المُحكَمة (بناء → فحص → تحقّق → مراجعة)، وأبلغ بالنتائج لا بالخيارات. **لا تعرض قوائم خيارات ولا تسأل «كيف/أيّهما نبدأ»** إلا عند قرار عملٍ مصيري غير قابل للتراجع (سياسة مالية أو حذف بيانات). الأولوية تتبع §٦ (الحالة الحيّة والقيود السارية) ثم دفاتر العمل المفتوحة في §٨.

**دورة كل جلسة (فريق وكلاء ذكي تحكمه القاعدة الحاكمة):** اختر مهمة → أنجزها **١٠٠٪ بكامل تفاصيلها** (خلفية + واجهة + شاشات + كل التحكمات والوظائف الإنتاجية والعملية، بأمان واعتمادية واستقرار) → **تحقّق + فحص + اصطياد الأخطاء وإصلاحها + متابعة** حتى التكامل الكامل → **commit + تقرير موجز ثم إنهاء الجلسة**. استعمل تنظيم الوكلاء (عمّال/مُحقِّقون/مكتشفو نواقص عبر Workflow أو مهارة `review-module`) **عند الحاجة** فقط (§٧) موازنةً مع الميزانية.

## ٥. اتفاقيات الكود (مُلزمة — يحرسها ٤٦٣ ملف اختبار + حرّاس §٣.١)
- **الذرّية:** كل عملية أعمال داخل `withTx(tx => …)` (server/services/tx.ts)؛ أي throw ⇒ ROLLBACK كامل.
- **المخزون:** بالوحدة الأساس (int) لكل (variant × branch) في `branchStock`. كل تغيير عبر `inventoryService.applyMovement` (يكتب حركة + يحدّث الرصيد تحت قفل `.for("update")`). `baseQuantity = quantity × conversionFactor` ويجب أن يكون صحيحاً.
- **الأموال:** كلها عبر `decimal.js` + `money.ts` (round2 HALF_UP، toDbMoney نصاً). **ممنوع** `parseFloat`/`Number` على الأموال.
- **⭐ المبدأ المالي الحاكم (نصّ المالك ٦/٨/٢٦): «لا دينار يضيع بصمت أو يُهدر أو يختفي أو ليس له مسار أو تبويب.»** كل مبلغٍ يدخل أو يخرج يجب أن يملك خمسةً معاً: (١) **إيصالاً** بمصدره (`receipts` بـdirection وcashBucket صحيحين)، (٢) **قيدَ دفترٍ مصنَّفاً** (entryType يضعه في تقريره الصحيح — أمانةٌ لا تُسجَّل إيراداً ولا العكس)، (٣) **أثراً في تسوية الدرج** إن مسّ النقد (computeExpectedCash/Z)، (٤) **طرفاً منسوباً إليه** (عميل/مورّد/مندوب/موظف)، (٥) **تقريراً يُظهره** يربطه بمستنده وفاعله. ومالٌ محتجَز (عربون/أمانة/عهدة) يلزمه **مسار خروجٍ ممكنٌ دائماً**: الثابت العمليّ `Σ(قيود الاحتجاز لكل مستند) = 0 ⇔ أُبرِئت`. أيّ شريحةٍ جديدة تُدقَّق على هذه الخمسة قبل الدمج — راجع ذاكرة [[money-trail-delivery-2026-08-06]] (٢٢ ثغرةً أُغلقت بهذا الميزان).
- **الدفتر (مبسّط، تلقائي):** قيد عند كل بيع/شراء/دفع/مرتجع. revenue=صافي قبل الضريبة، amount=الإجمالي، RETURN بقيم سالبة. الذمم: `customers.currentBalance` (AR موجب=مدين)، `suppliers.currentBalance` (AP).
- **المنتج:** `products` → `productVariants`(لون/قياس/تكلفة) → `productUnits`(قطعة/درزن/كرتون + معامل + باركود مستقل) → `productPrices`(سعر صريح لكل وحدة×فئة RETAIL/WHOLESALE/GOVERNMENT).
- **idempotency البيع:** `clientRequestId` → `invoices.sourceId` + قيد فريد `(sourceType,sourceId)` + إعادة محاولة الراوتر على `ER_DUP_ENTRY`.
- **حالة الفاتورة — قاموسٌ واحد ومجموعتان لا تُخلطان:** [`shared/invoiceStatus.ts`](shared/invoiceStatus.ts) هو **المصدر الوحيد** للتسمية العربية (`invoiceStatusLabel`) ولمجموعتَي الحالة، ويحرسه اختبارٌ نصّيّ: ⛔ **لا شاشة تُعيد تعريف قاموس الحالة محلّياً** (كانت سبعة قواميس منجرفة). و`PENDING` = **«غير مدفوعة»** لا «معلّقة» — بلاغ المالك: قرأها موظّفوه «قيد المعالجة/غير مؤكَّدة». والمجموعتان **ليستا مترادفتين**: `DEAD_INVOICE_STATUSES` (CANCELLED/RETURNED/SUPERSEDED) = مستندٌ لا يقبل **تحصيلاً جديداً** ⇒ فلاتر الذمم وربط السندات؛ و`VOIDED_INVOICE_STATUSES` (CANCELLED/SUPERSEDED **دون** RETURNED) = بيعٌ **لم يقع قطّ** ⇒ استبعاد الإيراد. خلطُهما يُسقِط المرتجع من الإيراد مرّتين (فهو مطروحٌ أصلاً بـ`returnedTotal`)، أو يُبقي فاتورةً مستبدَلةً فيه.
- **الطباعة:** `client/src/lib/printing/` — إيصال POS **بالتصميم المُعلَّم** عبر `printReceipt` (راسم نقطي `receiptRaster.ts` + قالب `printBrowserReceipt` كبديل متصفح — تصميم واحد على كل النواقل) بأولوية: جسر الخادم ← WebUSB ← المتصفح. Z-report/رصيد افتتاحي عبر `printDoc` العام. **⚠️ WebUSB على Windows يتطلب استبدال تعريف الطابعة بـWinUSB (أداة Zadig) وإلا `open(): Access denied`** — طُبّق على Epson TM-T20III بجهاز الكاشير (١٠/٦) وتحقّقت الطباعة العربية فعلياً.
- **بلا إيموجي في الواجهة:** حارس `scripts/check-no-emoji-ui.mjs` (يُفعَّل في CI عبر `pnpm check:emoji`) يمنع أي إيموجي في `client/**` — استبدلها بأيقونة من `lucide-react` (نمط `<Icon aria-hidden className="size-3" />`). أي إيموجي جديد يكسر CI فوراً على main. حالة سابقة: إيموجي `🔗` في [Vouchers.tsx:473](client/src/pages/Vouchers.tsx#L473) من ead9509 كسر CI على main (٥/٧)، عولج بأيقونة `Link2` (PR #134).

## ٦. الحالة الحالية (١٨ أغسطس ٢٠٢٦ — كل رقمٍ هنا مقيسٌ على `cf36dce`)

> **سجلّ الحملات المنتهية** (كل ما كان في هذا القسم: الأمانة، الأوفلاين، العمولات، واتساب، الافتتاح التدريجي، SOD، الأداء… ) نُقل **كما هو بلا حذف** إلى [`docs/campaign-log.md`](docs/campaign-log.md) — يُقرأ عند الحاجة لا في كل جلسة. هنا **الحالة الحيّة والقيود السارية** وحدها.

**🚀 الإنتاج:** `https://srv1548487.hstgr.cloud` على Hostinger VPS **مشترك** (⛔ سراج وأودو خطّ أحمر — لا يُلمسان؛ لا reboot/ufw/توقيت بلا موافقة المالك). MySQL حاوية على `127.0.0.1:3307` محجوبة عن الإنترنت + PM2 تحت مستخدم `deploy` (`pm2-deploy.service`، الويب cluster) + nginx/TLS/HSTS + نسخ ليليّ مشفَّر gpg + سحب يوميّ لجهاز المتجر (`backup:pull-vps`). **التحديث بأمر واحد: `pnpm prod:deploy`** — سكربت ذرّيّ (`git pull --ff-only` → `install --frozen-lockfile` → `db:backup` → `db:migrate:safe` → `db:verify` → `build` → `pm2 reload`) يتوقّف عند أوّل فشل بلا مسّ الخادم القديم. التفصيل: [`docs/deployment-vps.md`](docs/deployment-vps.md).

**🗃️ الهجرات:** ملفات SQL مولَّدة (`pnpm db:generate` محلياً) تُطبَّق عبر drizzle-orm migrator — آخرها **`0220_work_order_draft_link.sql`** (حملةُ الفواتير/الطلبات أخذت **0212-0220**، وmain أخذ 0210/0211؛ **ابدأ من 0221** بعد `git fetch origin main && pnpm check:migrations` — §٧-٤ب). ⛔ لا `db:push` عارياً على الإنتاج (حارس `db-push-guard.mjs`)، ولا `drizzle-kit migrate` (يفشل صامتاً).

**📱 تطبيق أندرويد أصيل «سوبر العربية»** (`android-native/`، Kotlin/Compose، مصادقة تشفيريّة بمفتاح جهاز EC P-256): على Google Play بمسار **Internal testing** فقط — **قرار المالك: لا Production ولا مراجعة Google** (الحساب شخصيّ). كل تحديثٍ للمختبِرين يلزمه رفع `versionCode` في **كل مواضعه المتزامنة** (`android-native/app/build.gradle.kts` بشقَّيه + `scripts/verify-mobile-release-env.mjs` + `.github/workflows/android-release.yml` — يحرسها `pnpm check:mobile-release`) ثم بناء AAB موقَّع عبر `android-release.yml` **ورفعٌ يدويّ** (CI لا يوصِل إلى Play).

**⚙️ وحدات مبنيّة كاملةً لكنها معطَّلة/مشروطة افتراضياً — لا تفترض أنها تعمل، ولا تحذفها ظنّاً أنها ميتة:**

| الوحدة | البوّابة الفعليّة |
|---|---|
| تعدّد الشركات (multi-tenancy) | `CONTROL_DATABASE_URL` — غيابها = المسار الأحاديّ، صفر أثر سلوكيّ |
| وضع الافتتاح (بيع الصنف غير المُفتتَح بالسالب) | `openingModeSettings` معطَّل افتراضياً + نافذة ≤٦٠ يوماً + نقديّ كامل فقط |
| البيع النقديّ دون اتصال | **مفعَّلٌ تلقائياً على كل جهاز** (قرار مالك ١٦/٨/٢٦ — كان معطَّلاً افتراضياً بمفتاحٍ لكل جهاز، فكان الكاشير المنقطع عاجزاً عن البيع لأنّ أحداً لم يُفعّله سلفاً). المفتاح صار **استثناءً** لاستبعاد جهازٍ بعينه. حرّاس المال لم تُمَسّ: عمر الأسعار ≤٤٨س · سقف الطابور · نقديّ كامل فقط · والالتقاط عند **فشل النقل فعلياً** لا بالاختيار. ⚠️ إعادة تحميل الصفحة **أثناء** الانقطاع تظلّ تتطلّب PIN جهازٍ مضبوطاً مسبقاً (يُضبط من شارة المزامنة على الجهاز نفسه — لا مسار خادميّ له) |
| مركز واتساب (Cloud API + المهام) | مدموج (هجرات 0106-0110)؛ صفر أثر بلا تكامل `ACTIVE`، وكل التدفّقات OFF |
| الدفتر المزدوج (P2) | الخرائط محجوبة؛ `postEntry` منفذ الكتابة الوحيد |
| مزامنة أجهزة الحضور | معطَّلة |

**💳 سياسة القبض — البوّابة إثباتٌ لا إقفال (١٦/٨، PR #601 منشور + #607):** مصدر الحقيقة الوحيد `shared/inboundPaymentPolicy.ts`؛ المفعَّل **CASH/CARD/TRANSFER/WALLET**، والمرفوضان بنيوياً **TELECOM** (مساره شاشة البطاقات الرقمية) و**CHECK** (قرار المالك). **كل شاشة تشتقّ طرقها من `isPosPaymentMethodEnabled`** — يحرس ذلك اختبارٌ صريح: ⛔ لا تُقفل طريقةً بنصّ ثابت (`disabled`/`!== "CASH"`) في شاشة. الإثبات مفروضٌ بحسب العقد: **محاولة `externalPaymentAttempts` مؤكَّدة** لـ`sales.create`/`printPos.createSale` (كاشير + مطبعة + فاتورة البيع المتقدّمة)، و**مرجعٌ نصّيّ** في نواة `createSaleInTx` (عرض سعر/حجز/أمر شغل) و`sales.pay` و`createVoucher` (+`cardLastFour` للبطاقة) و`walletOps.withdraw` بالتحويل. الثابت المحاسبيّ المحروس: غير النقد **`cashBucket=NULL`** ⇒ لا يَمسّ درج الكاشير ولا `expectedCash`.
> **سياق تحذيريّ:** #596 أقفل غير النقد إقفالاً شاملاً بعد ساعاتٍ من بناء مسار الإثبات في اليوم نفسه، فعطّل بيع البطاقة كلّياً وبثّ الإقفال في ~١٥ شاشة. الدرس: **لا تُفتَح طريقةٌ (أو ميزة) في شاشة قبل التحقّق أنّ عقد الخادم يقبل حمولتها فعلاً** — «الدالّة تُرجع true» ≠ «الطلب سينجح». التفصيل: ذاكرة [[inbound-payment-policy-2026-08-16]].

**📦 البكج (باندل):** تكلفته **صفرٌ بحكم التصميم** في `productVariants.costPrice` (يُركَّب لا يُشترى) — أيّ شاشةِ قراءةٍ تعرض ذلك العمود مباشرةً تُظهر هامشاً ١٠٠٪ كاذباً. المصدر الحاكم `loadBundleUnitCosts` (`bundleService.ts`) تستعمله شاشة المنتجات والكاشير كما يستعمله البيع. ⚠️ **لا تُلحق التكلفة في `applyBundleAvailability` المشتركة** — تخدم أيضاً `listStockByUnitIds` العائد بلا `redactPosCost` ⇒ تسريبها للكاشير. وطاقته مشتقّة من أضعف مكوّن، ويُفسَّر صفرها عبر `VariantAvailability.bundleCapacity` (`NO_RECIPE`/`COMPONENT_INACTIVE`/`COMPONENT_UNRESOLVED`/`COMPONENT_OUT_OF_STOCK` + **المكوّن الحادّ** باسمه وكمّيته). التفصيل: ذاكرة [[bundle-visibility-cost-and-reason-2026-08-16]].

**🔢 دقّة سعر الوحدة = دقّة العملة (١٧/٨):** مصدر الحقيقة الوحيد `shared/moneyPrecision.ts` — **الدينار منزلتان** (أعمدة `decimal(15,2)`) و**الدولار أربع** (`purchaseOrderItems.usdUnitPrice decimal(15,4)`؛ سعر الوحدة الدولاريّ يُشتقّ بالقسمة: 41.48 ÷ 12 = 3.4566). ثلاث طبقات كانت تقصّه إلى منزلتين صامتاً (خليّة السعر، حمولة الشاشة، `nonNegMoneyString` في الراوتر) فيرمي دقّةً صمّمت قاعدتُه لحفظها: على ٥٠٠٠ وحدة = ‎$17‎ تختفي من ذمّة المورّد وتُسمّم WAVG. القواعد السارية: ⛔ **لا `round2` على سعر وحدة** في حمولة شراء — استعمل `toUnitPriceStr(price, currency)`؛ عقد الراوتر `unitPriceString` (٤ منازل سقفاً) و**الخدمة تُضيّقه حسب العملة برفضٍ صريح لا تقريبٍ صامت**؛ وإجماليُّ السطر بالدينار **يُترجَم من إجمالي السطر بالدولار** لا من سعر الوحدة بعد تقريبه (الضرب بعد التقريب يُراكم الفرق × الكمية). وحقولُ المال في محرّر الفواتير كلّها `MoneyInput` — كانت خليّة السعر تُحلّل بـ`Number(v)` فتبتلع «1,450.99» والأرقام الهندية صامتةً (بلاغ المالك). الكاشير (`POS.tsx`/`Reception.tsx`) يبقى استثناءً مقصوداً: سعرٌ صحيحٌ بالدينار (`toDecimalPlaces(0)`) + تقريب ٢٥٠ للنقد.

**🧾 مطابقة فاتورة المورّد (١٧/٨):** حقل «قيمة فاتورة المورّد» في محرّر الشراء (`SupplierInvoiceMatch`) يُرسَل `supplierInvoiceTotal` **بعملة الأمر** (الدولاريّ بإجماليه الدولاريّ لأنّه مستند المورّد، والدينارّي بالدينارّي)، و`computePurchaseDocument` يرفض أيّ اختلافٍ برسالةٍ تحمل **الرقمين والفرق واتّجاهه وسببه المعتاد**. قبلها: لا حقلَ لها في أيّ شاشة، والشاشتان تمتنعان عن إرسال `usdTotal` هرباً من رسالةٍ عمياء («لا يطابق مجموع البنود») ⇒ **الضابط كلّه مُعطَّل وأمرُ الشراء يُحفَظ بلا أيّ تحقّقٍ من مستنده**. الفرق يُسوَّى بزرّ «وزّع الفرق على أسعار البنود» (`distributeToSubtotal`: تحجيمٌ بنسبة القيمة + آخر بندٍ يمتصّ الباقي) — **الأسعار الجديدة تظهر في الجدول قبل الحفظ**، ولا امتصاصَ خفيّ (§٥). وحين لا تبلغ حبيبةُ السعر × الكمّية الهدفَ، يُعلَن المتبقّي بدل ادّعاء تطابق. **وخصمُ فاتورة المورّد** صار ممثَّلاً (هجرة 0204، بقرار المالك ١٨/٨): يُدخَل **فاتورياً** بعملة الأمر ويُوزّعه `allocateByValue` بنسبة القيمة، فتُخزَّن أعمدةُ المال **صافيةً** ⇒ الذمّة (AP) وتكلفةُ المخزون (WAVG) ومرتجعُ الشراء تلتقطه **بلا تغييرٍ في أيٍّ من قرّائها** (`receive.ts` و`purchaseReturnsService` لم يُمَسّا). و`purchaseOrderItems.listUnitPrice`/`usdListUnitPrice` لقطةُ السعر **قبل** الخصم: بها يبقى المستند مطابقاً لورقة المورّد سطراً سطراً، وبها يُعيد المحرّرُ تحميلَ الأصل فلا يُخصَم مرّتين عند إعادة الحفظ. و`invoiceDiscount`/`usdInvoiceDiscount` على الأمر **إفصاحٌ لا مدخلُ حساب**. الضريبة على الوعاء **بعد** الخصم. ⛔ الخصم المتجاوِز لقيمة البضاعة مرفوضٌ بالرقمين (يقلب الذمّة والتكلفة سالبتَين).

**🧾 الفاتورة الآجلة — اشتقاقٌ لا حقل (١٨/٨، #636 + #638 منشوران):** جدول `invoices` **بلا عمود `paymentTerms`**؛ «الآجل» اشتقاقٌ من `paidAmount` مقابل الصافي، ومصدرُه الوحيد في الواجهة [`client/src/components/invoice/paymentTerms.ts`](client/src/components/invoice/paymentTerms.ts) (`derivePaymentTerms`: مسدَّدٌ بالكامل ⇒ نقدي، وإلّا آجل). ⛔ **لا تُسند شروط الدفع من قيمةٍ ابتدائية ثابتة** — كل مسارٍ يفتح فاتورةً قائمة (تعديل · تصحيح · «نسخ لفاتورة جديدة») يشتقّها صراحةً. الافتراض الصامت `"CASH"` في مُصغِّر المحرّر هو جذر بلاغ المالك: الفاتورة الآجلة تُفتَح «نقدية»، وحفظُها كان يُنشئ **قبضاً نقدياً كاملاً في الدرج** لأنّ `computePaidStr` يُرجع الإجماليّ للنقديّ حين يكون حقل المدفوع فارغاً.

**💵 ثوابت التحصيل والمرتجع والسداد (١٨/٨، #638) — خمسةٌ سارية، كلٌّ منها كان ثغرةً أثبتها اختبار:**
- **ربط السند بالفاتورة تخصيصٌ ماليّ لا حاشية توثيقية:** [`server/services/voucher/invoiceAllocation.ts`](server/services/voucher/invoiceAllocation.ts) هو **الكاتب الوحيد** لـ`invoices.paidAmount` من مسار السندات (قفل `FOR UPDATE` + رفض ما يتجاوز المتبقّي + رفض القبض على مستندٍ ميت). قبله: القبض يُنقص ذمّة العميل ولا يمسّ الفاتورة ⇒ تبقى «غير مدفوعة» فتُطالَب ثانيةً = **تحصيلٌ مزدوج**. والإنقاص (استرداد/عكس) **لا تحجبه حالةٌ أبداً** — لمالٍ خرج مسارُ رجوعٍ دائماً (§٥).
- **مرتجع الزبون العابر (بلا حساب):** لا يُحفَظ مرتجعٌ يُبقي في المكتبة مالاً هو للعميل — الردّ المطلوب ≥ `paidAmount − الصافي بعد المرتجع` (العابر بلا ذمّةٍ يُرحَّل إليها الفائض). ويُستثنى **عكسُ التصحيح الداخليّ** وحده بعلَمٍ داخليّ (`internalCorrectionReversal`) لا يبلغه الراوتر.
- **`purchases.pay`** ([`server/services/purchase/pay.ts`](server/services/purchase/pay.ts)): الشراء الآجل صار له نظير `sales.pay`. **بلا هجرة ولا عمود** — يعيد استعمال آلية `PURCHASE_SUPPLIER` نفسها ⇒ الأثر عند **الاعتماد** (Maker-Checker) و`paidAmount` يتحرّك تحت القفل. قبله: كلّ سدادٍ بعد الاستلام يخرج إلى سند صرفٍ عامّ لا يمسّ `purchaseOrders.paidAmount` ⇒ «المتبقّي» يطالب بمبلغٍ مسدَّد، وخطرُه **دفعٌ مكرَّر للمورّد**.
- **`invoices.paymentMethod` قد تكون `MIXED`** — قيمةُ **عرضٍ مشتقّة** تكتبها الخدمة عند تعدّد روافد السداد (`mixedAwarePaymentMethod`)، لا خياراً في أيّ شاشة (`PosPaymentMethod` تستثنيها كما تستثني CHECK/TELECOM/EXCHANGE). وفلترُ الطريقة يعالج `NULL` صراحةً (فاتورةٌ لم تُقبَض بعد) بدل إسقاطها من النتيجة.
- **`sales.correct` معزولٌ فرعياً** (`canCrossBranches`) ويرفض المستند الميت، و**العمولة تتبع البائع الأصليّ**: التصحيح يورّث `invoices.createdBy` عبر `attributeToUserId` الداخليّ بدل نسب المبيعة إلى المصحِّح.

> التفصيل الكامل للحملة (الجذور والأدلّة والدرسان التشغيليّان) في مدخل ١٧-١٨/٨ بـ[`docs/campaign-log.md`](docs/campaign-log.md).

**⚖️ قرارات المالك السارية (لا تُنقَض بلا طلبٍ صريح):**
- **الضريبة** 0% غير شامل · **التكلفة** المتوسّط المرجّح (WAVG) · **الدفع الزائد** مسموح.
- **سقف الائتمان مُفعَّل** (`server/lib/credit.ts`): `null` = بلا حدّ، و`"0"` = نقديّ فقط — **وهو الافتراضي**؛ الوضع الصريح محصورٌ بالمدير.
- **الشحن/الكمرك** مصروفُ شركةٍ يُعترَف به **لحظة الاستلام**؛ لا يمسّ ذمّة المورّد ولا تكلفة الصنف (تكلفة الصنف = سعر المورّد وحده)، بلا تصحيح رجعيّ.
- **التوصيل المجّاني إفصاحٌ لا محاسبة:** `deliveryFree`/`deliveryWaivedAmount` لا يزيدان `total` ولا `revenue` ولا يُنشئان قيداً.
- **الهدايا:** تكلفة السطر المُهدى في قيد `GIFT_OUT` وحده (لا في `SALE`)، وخارج وعاء العمولة.
- **بضاعة الأمانة:** حصّة المودِع في `costPrice`، «طريقة الإجمالي»، العمولة على **الهامش** فقط، والتلف/الضياع على المكتبة.
- **فصل المهام (SOD):** مُنشئ ≠ مُعتمِد على الصرف وتسوية المخزون ونهاية الخدمة (admin مُستثنى للتصحيح الإداريّ).
- **عزل مدير الفرع:** لا عبور بين الفروع — `canCrossBranches` = admin/isOwner فقط (`server/lib/branchAuthority.ts`).
- لا تعامل بالصكوك · المطبعة **ديجيتال** لا أوفست · إلغاء فاتورة البيع بمسارٍ موثَّق (عكسٌ كامل) · الدفعة الأولى لخطة القسط بلا سند (حارسٌ موحَّد: خادم + ويب + أندرويد).

**🧭 اتجاه العمل الأخير (من `git log`، ١٣-٢٠/٨):** حملةُ منظومة الفواتير والطلبات (§٨) · بلاغ «الفاتورة الآجلة تظهر معلّقة» وعنقودُه الجنائيّ (#636 ثمّ #638: السندات والمرتجعات والمشتريات) · تمريرُ `--shard` فعلياً في CI بعد أن كانت التجزئة خاملة (#637) · دقّة سعر الوحدة حسب العملة ومطابقة فاتورة المورّد (#640) ثمّ تمثيلُ خصم فاتورة المورّد بهجرة 0204 (#644) · فئات السندات والمصروفات (#639) · تجميد الشهر المصروف في المسيّر (#634-#635) · إعادة بيع البطاقة وإظهار كلفة البكج وسبب نفاده (#601، منشور) ثمّ فتح غير النقد على الشاشات الأربع الباقية بتمرير إثباتها (#607) · حوكمة الورديات والدرج النقديّ (#596-#599: تمويلٌ إضافيّ محكوم + معالجة الدرج السالب الموروث + إنفاذ سلامة النقد والمخزون) · إعلانات الموظفين خلفيةً وواجهةً أصيلة (#589-#594).

**⚠️ ملاحظة صحّة:** أيّ ادّعاء «مكتمل/منشور» في `docs/campaign-log.md` مؤرَّخٌ بلحظة كتابته — **تحقّق من الشيفرة قبل البناء عليه**، فقد تجاوزته حملاتٌ لاحقة.

## ٧. تنسيق الوكلاء والجلسات (منع التعارض + ذكاء الأعمال)
**نظام الوكلاء (عبر أداة Workflow):**
- **عمّال (workers):** يكتبون الكود. التوازي على ملفات مختلفة فقط، وبعزل **worktree** (`isolation: "worktree"`). **كاتب واحد لكل ملف/وحدة**.
- **مُحقِّقون (verifiers):** مراجعة عدائية متعددة العدسات (صحة/أموال/ذرّية/أمان/UX)، **للقراءة فقط** → يحكمون على كل ملاحظة (تأكيد/رفض). لا يعدّلون ⇒ لا تعارض.
- **مكتشفو النواقص (gap-finders):** ناقد اكتمال يسأل «ما الناقص؟ شاشة؟ تدفّق؟ ادعاء غير مُتحقَّق؟» → يغذّي الجولة التالية.
- النمط القياسي لكل وحدة: تصميم (لوحة خبراء) → تنفيذ (عامل) → تحقّق (عدائي) → كشف نواقص → إصلاح. أمثلة مُطبَّقة: ورشة تصميم العمود الفقري، ومراجعته العدائية التي كشفت ٧ أخطاء.

**🤝 بروتوكول الفريق (التوازي بلا تعارض = عزل + تنسيق + دمج — الطبقات الثلاث):**
> العزل وحده لا يكفي؛ التوازي الآمن يحتاج **قائداً واحداً ينسّق**، لا عدّة جلسات متكافئة تتسابق.
- **١) قائد واحد (orchestrator):** جهةٌ واحدة (جلسة قائدة أو سكربت Workflow) تُحلّل الشريحة، **تعرّف العقود** (شكل الواجهة بين الـAPI والشاشة)، توزّع **وحدات غير متقاطعة**، ثم تدمج. لا وكيل يختار لنفسه عملاً يتقاطع مع غيره.
- **٢) العقد قبل التوازي:** القائد يثبّت شكل المدخلات/المخرجات بين الطبقات **قبل** إطلاق العمّال ⇒ تبني الشاشة على عقدٍ متّفقٍ عليه فيتكامل مع الـAPI بلا انجراف.
- **٣) ملكية ملفات صريحة:** كل وحدة تُعلن ملفاتها — **كاتب واحد لكل ملف**. وحدتان لا تكتبان نفس الملف أبداً (لا حتى في worktrees منفصلة، وإلّا فتعارض دمج).
- **٤) 🔥 سجلّ الملفات الساخنة (يملكها القائد وحده، يعدّلها تسلسلياً بعد فراغ العمّال):** `server/routers.ts`، `client/src/App.tsx` (المسارات)، `client/src/components/AppLayout.tsx` (التنقّل)، `drizzle/schema.ts`، `drizzle/migrations/meta/_journal.json`، `server/seed.ts`. **الوكلاء/الشرائح لا يلمسونها** — هي مغناطيس تعارض الدمج.
- **٤-ب) 🔢 ترقيم الهجرات يُحجَز لا يُخمَّن (١٨/٨):** ثلاثة فروع متوازية أخذت **`0204`** نفسه في يومٍ واحد (#644 و#645 و#648) لأنّ كلاًّ منها فرّع من `main` قبل دمج الآخر، ولا يظهر التصادم إلّا عند الدمج. القاعدة: **`git fetch origin main` ثمّ `pnpm check:migrations` قبل توليد أيّ هجرة**، ورقمُك = آخر رقمٍ على `main` + عدد الأرقام المحجوزة لفروعٍ مفتوحة. **المستعمل على `main`: `0204` (#644) و`0206` (#648) و`0207` (#645) — ابدأ من `0208`** (و`0205` بقي شاغراً بلا ملفّ). وإعادةُ الترقيم رخيصةٌ ما دامت الهجرة غير مدموجة: بدّل اسم الملف ومدخل `_journal.json` (`idx` و`tag` و`when`) معاً — و**`when` هو الحاسم لا الرقم**: drizzle يطبّق الهجرة فقط إن كان `when` أكبر من أكبر `created_at` مُسجَّل ([`mysql-core/dialect`](node_modules/drizzle-orm/mysql-core/dialect.cjs): `lastDbMigration.created_at < migration.folderMillis`) ⇒ هجرةٌ أُعيد ترقيمها بـ`when` **أقلّ** من هجرةٍ سبقتها إلى `main` **تُتخطّى صامتاً فلا تُطبَّق أبداً**. لذا أبقِ ترتيب الأرقام موافقاً لترتيب `when` دائماً.
- **٥) الدمج + المزامنة مع الريموت:** شرائح صغيرة، دمج متكرّر إلى `main`؛ القائد يطبّق تعديلات الملفات الساخنة + `pnpm check` + الاختبارات + جولة بصرية قبل الإغلاق، **ثم يدفع فوراً: `git push origin main`** ليبقى الريموت (`origin` = GitHub: ahrrfy/business_management_system) مطابقاً للمحلي. القاعدة: **كل دمج إلى `main` يُتبَع بدفعٍ إلى `origin/main`** (المحلي والريموت يتقدّمان معاً دائماً). الدفع بعد التحقّق فقط — لا يُدفع عملٌ غير مدموج/غير مُختبَر. التفصيل: [[github-remote]] في الذاكرة.

**🔒 الإنفاذ الميكانيكي (نظام coord) — يحوّل البروتوكول أعلاه من نيّةٍ إلى قانون:**
- **قفل ذرّي للشريحة:** `pnpm coord:claim <شريحة> --files <أنماط>` يكتب قفلاً بـ `O_EXCL` في سجلّ مشترك خارج كل worktrees ⇒ كاتبٌ واحد يفوز، **لا سباق ممكن** حتى عند بدءٍ متزامن (يغلق ثغرة ٦/٦). الأوامر: `coord:list|status|release|reclaim|heartbeat`. السجلّ: `~/.claude/coord/<projectKey>/` (أقفال بمهلة ١٥د + استرجاع متحفّظ للمعطوب).
- **hooks تمنع فعلياً** (`.claude/settings.json`): `PreToolUse` يرفض الكتابة على `main`، أو على ملفٍ تملكه جلسة أخرى حيّة، أو على ملف ساخن لغير مالك `_integration`؛ و`SessionStart` يحقن الصورة الحيّة + تحذير main. **يفشلان مفتوحَين** (لا يعطّلان عملاً مشروعاً) ومخرج طوارئ للمالك `COORD_BYPASS=1`.
- **ورشة الشريحة:** مهارة `build-slice` + `‎.claude/workflows/build-slice.js` تشغّل النمط القياسي كاملاً (عقد→كتّاب متوازون→تحقّق عدائي عبر `review-module`→نواقص→إصلاح→دمج) مع **بوّابة DoD آلية** (لا «نجاح» بلا خلفية+واجهة+فحص+اختبار+تنقّل+جولة بصرية). التفصيل التشغيلي في `docs/playbook.md`.

**🤝 التوافق والتنسيق مع Codex (وكلاء OpenAI Codex يعملون على هذا المستودع فعلياً، بفروع `codex/*`):**
- **Codex يقرأ `AGENTS.md` لا `CLAUDE.md`.** الجذر يحمل `AGENTS.md` **مؤشّراً رقيقاً** إلى هذا الملف — ⛔ **لا تنسخ محتوى `CLAUDE.md` إليه**: النسخ تنجرف حتماً (وُجدت في ١٦/٨ نسخٌ محلّية غير متتبَّعة بتاريخ ١/٨ في ٧ أشجار، بينما الأصل تقدّم ٢٥٢ التزاماً ⇒ كان Codex يبني على حقيقةٍ مجمَّدة). **مصدر الحقيقة واحد: `CLAUDE.md`.**
- **البروتوكول واحدٌ للطرفين:** جلسات Codex تشارك في coord فعلياً وتظهر في `pnpm coord:list` بادّعاءاتها وملفاتها ⇒ قبل أيّ كتابة افحص `coord:list` + `git status`، وادّعِ شريحتك، **ولا تلمس ملفاً تملكه جلسة حيّة أياً كان محرّكها** (Claude أو Codex). القفل الذرّي لا يفرّق بينهما.
- **قبل الدمج:** كلا المحرّكين يمرّان بنفس البوّابة — `pnpm check` + `pnpm check:guards` + CI أخضر. لا مسار جانبيّ لأحدهما.
- `.agents/` و`.codex/` حالةُ أدواتٍ محلّية لكل شجرة (مُستثناة من git) — لا تُلتزَم ولا يُبنى عليها.

**عزل الجلسات (git) — إلزامي ومُفعَّل آلياً (انظر «بوّابة بدء الجلسة» §٤):**
- كل جلسة على **فرع + شجرة عمل معزولة** عبر `pnpm session:new <اسم>` (ينشئ `session/<اسم>` + مجلّداً مجاوراً بفهرس git خاص + **قاعدة `erp_<اسم>` و`erp_<اسم>_test` ومنفذاً و‎.env مستقلّين** + يسجّل الجلسة في coord) — **الكتابة على `main` محظورة بـ hook**. للعرض: `pnpm session:list`، وللإزالة بعد الدمج: `pnpm session:remove <اسم>`. الدمج إلى `main` بعد بلوغ ١٠٠٪+تحقّق فقط.
- **قبل أي كتابة:** اسرد الجلسات النشطة + اقرأ `git status`. **لا جلستان تُعدّلان نفس الوحدة، ولا جلستان تتشاركان شجرة عمل/فهرس git على نفس الوحدة.** إن وجدت WIP غير ملتزم لغيرك (أو ملفات جهّزها غيرك في الفهرس): **توقّف وأبلغ المالك** — لا تُكمل ولا تطمس ولا تُرجِع عمله.
- **عزل القاعدة تلقائي:** `session:new` يُنشئ `erp_<اسم>` (تطوير) و`erp_<اسم>_test` (اختبار) لكل جلسة ويكتبهما في `.env` ⇒ زال تصادم قاعدة `erp`/`erp_test` المشتركة. (مع Docker متوقّف: تحذير + بديل يدوي.) لكل جلسة منفذها الخاص أيضاً.
- **تغييرات المخطط:** مالك واحد على فرع واحد في كل مرة (تجنّب تعارض الهجرات).
- **تتبّع الملكية عبر سجلّ coord الذرّي** (`coord:claim`) لا بالاتفاق الطوعي — هو مصدر الحقيقة لمن يملك أي شريحة/ملفات. (نظام المهام TaskCreate/`owner` يبقى للتتبّع داخل الجلسة.)

## ٨. مراجع
- **دليل النظام التشغيلي العميق** (تمتين الذاكرة/التعارض/الوكلاء): [`docs/playbook.md`](docs/playbook.md) — يُقرأ عند الحاجة. **أبقِ هذا الملف رشيقاً.**
- **سجلّ الحملات المنتهية:** [`docs/campaign-log.md`](docs/campaign-log.md) — أرشيف §٦ و§٩ السابقين حرفياً (٩٧ك). تاريخيٌّ لا مرجعيّ: **تحقّق من الشيفرة قبل البناء على أيّ ادّعاءٍ فيه.**
- **دفاتر عملٍ مفتوحة:** *(لا يوجد حالياً بندٌ مُسمّى ومُتحقَّقٌ منه مفتوح — انظر الحملة الجارية أدناه)*
  > ⚠️ **قاعدةٌ تعلّمناها بالثمن (١٨/٨):** هذا السجلّ **يشيخ أسرع من الشيفرة**. ظلّ يُسمّي بندَين
  > «متبقّيَين جوهريَّين» وكلاهما مُغلقٌ منذ مدّة، فكاد يُستهلك عليهما جهدُ جلسةٍ كاملة.
  > **قبل بناء أيّ شيءٍ على بندٍ هنا: افتح الشيفرة وتحقّق أنه ما زال قائماً.** وإن وجدتَه
  > مُغلقاً فانقله إلى الأرشيف أدناه بدليله — تركُه يُضلّل من بعدك (Claude وCodex معاً).
- **الحملة الجارية (١٨-٢٠/٨) — منظومة الفواتير والطلبات** (فرع `claude/invoices-orders-system-149fd0`،
  هجرات **0212-0220**): سبعُ مراحلَ بإطار المالك من الفحص الجنائيّ إلى إغلاق آخر بابٍ مسدود.
  التفصيل الكامل في [`docs/campaign-log.md`](docs/campaign-log.md) — وهذه **القيود السارية** منها:
  - **ثلاثةُ قواميس حاكمة جديدة** يحرس كلاًّ منها اختبارٌ نصّيّ: [`shared/receptionChannel.ts`](shared/receptionChannel.ts)
    (كانت تسعَ تعريفات) · [`shared/invoiceChannel.ts`](shared/invoiceChannel.ts) (قناةُ الفاتورة
    **اشتقاق** من `sourceType`+`shiftType`، ويُثبت اختبارٌ أنّ SQL الخادم ≡ الاشتقاق المشترك) ·
    [`shared/workOrderStatus.ts`](shared/workOrderStatus.ts) (تسعةُ قواميس حالة). ⛔ **لا شاشة
    تُعيد تعريف أيٍّ منها محلّياً.**
  - **`workOrders.reverseDelivery`** هو **المخرجُ الوحيد** لأمرٍ مُسلَّم: الإلغاء يرفض المُسلَّم،
    و`sales.cancel`/`correct` يرفضان WORKORDER، و`returns.create` يشترط بنداً وفاتورةُ الخدمة
    الخالصة **بلا بنود**. وprofile `RETURN_SALE_FLEX_WORKORDER` مرآةُ `SALE_SERVICE_FLEX` دوراً
    بدور — `RETURN_SALE_FLEX` القائم لا يكفي (لا مكانَ فيه لعكس COGS/WIP ولا لإعادة فتح الأمانة).
  - **هدرُ الإلغاء قيدٌ فقط** (`ADJUST_WIP_WASTE`: LOSSES/WIP): ⛔ لا `createStockExpenseTx` ولا
    أيّ حركة مخزون — المادّة خُصمت عند البدء، وخصمُها ثانيةً سالبٌ كاذب. وقرارُ الخامة **تصريحيّ**
    مجموعُه = المستهلَك بالضبط، وحذفُه كلّياً = رجوعٌ كامل (السلوك القائم).
  - **`invoices.shiftId = NULL`** حالةٌ مشروعة (تسليمٌ بلا وردية RECEPTION): يستثنيها
    `saleRouter` و`reception/queries.ts` و`reception/collect.ts` **معاً** بشرطٍ واحد
    (`shiftId IS NULL AND sourceType='WORKORDER'`). الشرطُ يتغيّر في الثلاثة معاً أو لا يتغيّر:
    إظهارُ صفٍّ يُرفض قبضُه أسوأ من إخفائه. والحصرُ بالمنشأ يمنع ابتلاع الطابور.
  - **فخُّ اختبارٍ يُنتج أخضرَ كاذباً:** `accountingEntries.postingProfile` **لا يُملأ** إلّا مع
    الدفتر المزدوج (OFF افتراضياً) ⇒ التأكيدُ عليه يمرّ فارغاً دائماً. ميّز القيود بـ`dedupeKey`.
  - **الأدوارُ المخصّصة تحمل `baseRole`**: فنّيٌّ بدورٍ مخصّص يبقى `role = 'print_operator'` —
    فلا تُوسّع بوّابةً بحجّة أنّه «محجوب»، وهو ليس كذلك (أمسكه `rbacTightening`).

- **الحملة المنتهية (١٧-١٨/٨) — سلسلة الحضور والأجر** (الفحص الجنائيّ ١٧/٨ + قرارات المالك الثلاثة):
  - **مدموجٌ في `main`:** #633 (ستّة أعطاب في محرّك الأجر والطيّ · **ق٢**) · #634 (اليوم المفتوح
    يُوسَم لا يُستبعَد · **ق١**) · #635 (الشهر المصروف مجمَّد · **ق٣**) · #641 (`pnpm audit:payroll`
    مدقّقُ قراءةٍ محضة على بيانات الإنتاج) · #648 (حوكمة إعادة تقييم التكلفة + إقفال
    الفترة يحرس المخزون — هجرة 0206) · #650 (قاعدة حجز ترقيم الهجرات) · #653 (سلطة التسعير اليدويّ — خصم الرأس وقناة الطباعة) · **#645** (بند ٢١، هجرة 0207).
    و**#649** بند ٤٣ — أجر الشهر الأخير في تسوية نهاية الخدمة يُطابَق باشتقاق المحرّك.
  - **لم يبقَ من هذه الحملة شيءٌ مفتوح.** ⛔ ولا تُضِف هنا قائمةَ PRات مفتوحة: قائمةٌ كهذه
    تكذب خلال ساعات (هذا السطر نفسه كان يُسمّي #649 «مفتوحاً» ودُمج قبل أن يُدمج نصُّه).
    حالةُ العمل الجاري مصدرُها `git log origin/main` و`gh pr list` لا هذا الملف.
  - بقيّةُ ملاحظات الفحص الجنائيّ **لم تُعَدّ ولم تُتحقَّق حديثاً** — لا تبنِ على عددٍ منها،
    اشتقّه من الشيفرة عند الحاجة.
- **أرشيف مُغلق — لا تُعِد استقصاءه:**
  - [`docs/financial-integrity-audit-2026-07-27.md`](docs/financial-integrity-audit-2026-07-27.md) — تدقيق ٦٨ وكيلاً؛ صفر حرِج، ٨ عالية. **مُغلق (١٨/٨) بالتحقّق من الشيفرة:**
    - بندا عزل RBAC أُصلحا (١٦/٨): `globalSearch/rbac.ts` يستشير خريطة الصلاحيات بدلالة `requireModule`، وحارس `shift.report` يقرأ `report.shift.branchId` ([shiftRouter.ts:477](server/routers/shiftRouter.ts#L477)).
    - **حوكمة تعديل `costPrice`** — مُغلقة: مسارا التعديل اليدويّ ([`catalog/productUpdate.ts`](server/services/catalog/productUpdate.ts) و[`productEditService.ts`](server/services/productEditService.ts)) يستدعيان `postCostRevaluation`، وهي **تفشل مغلقةً**: تعديلُ تكلفة صنفٍ مملوكٍ له رصيدٌ غير صفريّ يُرفَض أصلاً ويلزمه استلامُ شراءٍ أو جردٌ معتمد ([`costRevaluation.ts`](server/services/costRevaluation.ts))، ويُكتب أثرٌ مُهيكَل داخل المعاملة، ويلزم `assertCostChangeReasonOrThrow` سبباً للتغيّرات الكبيرة. فلا «حركة حقوقٍ صامتة» ولا COGS رجعيّ. و**#648 أكمل الطرف الناقص**: طريقٌ محكومٌ لتصحيح تكلفةٍ خاطئة على صنفٍ قائم (`costRevaluationRequests`، هجرة 0206) بمستندٍ واعتمادٍ ثانٍ وقيدٍ لكلّ فرعٍ له رصيد — فلم يعد الإغلاقُ طريقاً مسدوداً بلا بديل.
    - **التسعير اليدويّ بلا سقف (H6/H7)** — مُغلق **على مرحلتين**، والتفصيل يهمّ لأنّ المرحلة الأولى وحدها لم تكن كافية:
      - **بوّابة السطر** ([`billing.ts`](server/services/billing.ts)): انحرافُ صافي السطر عن **سعره المرجعيّ** فوق `MANUAL_DISCOUNT_APPROVAL_THRESHOLD` (١٥٪) ⇒ `FORBIDDEN` بلا اعتماد مدير. هي التي أغلقت **H7** (إهداءُ صنفٍ مُدرَجٍ بسعرٍ = انحرافٌ ١٠٠٪) وأزالت جوهر **H6** (القياس صار على المرجع لا على التكلفة: «٥٠٠٠ تكلفته ١٠٠٠ يُباع بـ١٠٠١» = انحرافٌ ٨٠٪ ⇒ تفويض).
      - **و#653 أغلق النصف الباقي من H6**: خصمُ **رأس الفاتورة** كان يفلت من بوّابة السطر كلّياً (`computeInvoiceTotals` يقصّه إلى `[0, subtotal]` وحسب ⇒ خصمٌ فاتوريّ ٤٠٪ يمرّ بلا اعتماد ما دام الصافي فوق التكلفة)، فصار يُقاس صافي الفاتورة مقابل مرجعها الإجماليّ بالعتبة نفسها ويتراكم مع خصم السطر؛ وقناةُ **المطبعة** كانت تحرس تحت-التكلفة وحدها فوُوئمت. ووسمُ `priceOverride` صار يشمل التنازل فوق التكلفة لا التحت-التكلفة فقط — وكان غيابُه شكوى H6 الثانية.
      - تحرسها `manualPriceGate.test.ts` و`manualPricingAuthority.test.ts`.
      > ⚠️ **حافّةٌ متبقّية مقصودة (باقيةٌ بعد #653):** البوّابتان تُرجعان `false` حين لا مرجعَ موجب (`refGross ≤ 0`) — أي صنفٌ بلا سعر قائمةٍ ولا سعرِ عقد. يبقى عليه حارسُ تحت-التكلفة وحده، فصنفٌ بلا مرجعٍ **وبلا تكلفة** معاً يمرّ. ضيّقةٌ لكنّها ليست معدومة — لا تدّعِ إغلاقاً مطلقاً.

  - [`docs/functional-audit-2026-07-17.md`](docs/functional-audit-2026-07-17.md) — **مُغلق (١٨/٨)**: بندُه الأخير («رقم أمر الشراء يقود إلى `/purchases/:id` غير المعرَّف») انتُهي منه — المسار معرَّفٌ في [App.tsx:322](client/src/App.tsx#L322) بـ`PurchaseOrderDetail` **بعد** `/purchases/new` و`/:id/receive` و`/:id/edit` عمداً (العامّ لا يبتلع الأخصّ منه). وقرارات المالك التي كانت معلَّقة فيه حُسِمت كلّها (إلغاء الفاتورة #563 · عزل مدير الفرع #568 · الدفعة الأولى للقسط #560).
  - [`docs/_audit-archive/REVIEW-2026-06-15.md`](docs/_audit-archive/REVIEW-2026-06-15.md) (مُعالَجٌ بالكامل).
  - [`docs/audit-followups-2026-07-02.md`](docs/audit-followups-2026-07-02.md) — **مُغلق**: بنود F1-F7 كلّها `[x]` (١٤ مربّعاً، صفر غير مشطوب) والملف نفسه ينتهي بـ«يمكن أرشفة هذا الملف بعد الدمج». لا تُعِد فتح هجرة FK أو إنفاذ الأدوار أو idempotency العروض بناءً عليه.
- **لا يوجد `README.md`:** هذا الملف هو المدخل الوحيد للمشروع، و`docs/` يحوي ٣٥+ وثيقة تصميمٍ وتدقيقٍ مؤرَّخة.
- **ذاكرة دائمة:** `~/.claude/projects/.../memory/` (مؤشرات). **خطط المراحل:** `~/.claude/plans/`.
- **اختبارات:** `server/services/__tests__/` (الثقل الأكبر) + `client/src/lib/**/*.test.ts` + `shared/**/*.test.ts`.
