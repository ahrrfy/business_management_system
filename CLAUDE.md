# CLAUDE.md — نظام إدارة أعمال الرؤية العربية

> هذه الوثيقة هي **مصدر الحقيقة** للمشروع. تُحمَّل تلقائياً لكل جلسة/وكيل. اقرأها أولاً قبل أي عمل — تُغني عن إعادة اكتشاف السياق (توفير ميزانية).

## ١. هوية المشروع
- الشركة: **الرؤية العربية للتجارة العامة — المكتبة العربية للطباعة والقرطاسية**.
- السوق: **العراق** (عربي RTL، عملة دينار عراقي IQD، **بلا ضريبة VAT** افتراضياً = معدّل 0%).
- النشاط: مطبعة + مكتبة قرطاسية، **فرعان** (رئيسي MAIN كل الخدمات + فرع مبيعات SALES). قطاعات: طباعة، قرطاسية (مفرد/جملة)، هدايا/تخرج، تجهيزات مكتبية، عملاء أفراد/تجار/شركات/دوائر حكومية.
- v2 **إعادة تأسيس نظيفة** بعد مشروع Manus (الإرث مؤرشف في `_legacy/`، مُستثنى من git).

## ٢. التقنية والبنية
- React 19 + Vite + Tailwind v4 + shadcn/ui + wouter + TanStack Query؛ tRPC v11؛ Express؛ Drizzle ORM (mysql2) + MySQL 8؛ decimal.js؛ TypeScript.
- مصادقة محلية مملوكة (JWT عبر jose + scrypt) — **لا OAuth ولا Manus**.
- **مسار الطلب — اقرأه مرّةً، يُغنيك عن تتبّع عشرات الملفات:**

```
client/src/pages (٢٣٧ صفحة، wouter + TanStack Query)
   └─ client/src/lib/trpc.ts ──POST /api/trpc──▶ server/routers.ts (برميل يجمع ٨٣ راوتراً)
        └─ server/routers/*  : تحقّق zod + اختيار الإجراء المُبوَّب (بلا منطق أعمال)
             └─ server/services/*  : منطق الأعمال داخل withTx (١٦٣ ملفاً/حزمة)
                  └─ drizzle/schema.ts (١٩٠ جدولاً، ٨٦٠٠ سطر) ──▶ MySQL 8
```

- **قاعدة الطبقات:** الراوتر بلا منطق أعمال، والخدمة **لا تقرأ `ctx`** — تستقبل `Actor { userId, branchId, role }` (`server/services/tx.ts`). كسرُ هذا يُعمي حرّاس الصلاحيات ويجعل الخدمة غير قابلة لإعادة الاستعمال من قناةٍ أخرى (أوفلاين/أندرويد/استيراد).
- **مفردات التفويض = `server/trpc.ts`** (٨٠ إجراءً مُصدَّراً جاهزاً): **لا تكتب فحص صلاحيةٍ جديداً — اختر الإجراء المناسب.** الاصطلاح `query ⇒ READ` و`mutation ⇒ FULL`؛ `moduleProcedure(roles, moduleKey, level)` = بوّابة وحدة + إلزام فرعٍ مُسنَد؛ و`branchScopedProcedure` يحقن `scopedBranchId` (عزل الفرع) و`scopedOwnerId` (عزل سجلّات الموظف: يرى ما أنشأه فقط). الخريطة المشتركة مع الواجهة في `shared/permissions.ts` — والإنفاذ النهائيّ **خادميّ دائماً**.
- **المسارات المختصرة:** `@/*` ⇐ `client/src/*` و`@shared/*` ⇐ `shared/*` (مضبوطة في `tsconfig.json` + `vite.config.ts` + كلا ملفَّي vitest).

## ٣. التشغيل (Windows + PowerShell)
```
docker start erp-mysql            # حاوية MySQL (mysql:8, root/erp_root_pw, منفذ 3306, قاعدة erp)
pnpm install
pnpm db:push                      # هجرة المخطط (محروسة بـ scripts/db-push-guard.mjs)
pnpm seed                         # admin + فرعان + منتجات عيّنة (idempotent)
pnpm dev                          # http://localhost:3000
pnpm check                        # فحص الأنواع (tsc --noEmit)
pnpm check:guards                 # حزمة الحرّاس العشرة — بوّابة ما قبل الدفع (§٣.١)
pnpm test:db:init                 # تهيئة قاعدة اختبار هذه الشجرة طازجةً (بعد تغيّر المخطط)
pnpm test                         # الحزمة الكاملة (vitest، TZ=UTC، ٤٦٣ ملف اختبار)
pnpm test:unit                    # ٤١ ملفاً منطقياً بلا قاعدة — ثوانٍ، للتكرار السريع
pnpm build && pnpm start          # بناء الإنتاج (vite + esbuild) ثم تشغيله من dist/
pnpm format                       # prettier
```

**تشغيل اختبارٍ واحد** (الحزمة الكاملة ٢٠-٤٥ دقيقة — لا تشغّلها للتحقّق من تغييرٍ صغير):
```
pnpm exec cross-env TZ=UTC vitest run server/services/__tests__/sale.test.ts
pnpm exec cross-env TZ=UTC vitest run server/services/__tests__/sale.test.ts -t "اسم الحالة"
```
- **`TZ=UTC` إلزاميّ:** سكربت `test` يضبطها بـ`cross-env`، والاستدعاء المباشر لا ⇒ تُحمَّر اختبارات التاريخ زوراً.
- **قاعدة الاختبار تُشتقّ من اسم شجرة العمل تلقائياً** (`scripts/lib/test-db-name.mjs`): `erp_<اسم-المجلّد>_test` على `127.0.0.1:3310`، والمستودع الأصل وحده يبقى `erp_test`. هذا ما يمنع تقاتل الجلسات المتوازية على قاعدةٍ واحدة (`__setup__.ts` يمسح **كل صفوف كل الجداول** بعد كل ملف) — لا تضبط `TEST_DATABASE_URL` يدوياً بلا سببٍ صريح.
- `fileParallelism: false` — الملفات تتسلسل؛ لا تسريع بالتوازي.
- ⚠️ **`tsconfig.json` يستثني `**/*.test.ts`** ⇒ `pnpm check` **لا** يفحص أنواع الاختبارات؛ خطأ النوع فيها يظهر عند `vitest` وحده.
- **`pre-commit` = `pnpm check && pnpm check:guards`** (~دقيقة) لا الحزمة الكاملة — أُعيدت معايرته لأنّ الحارس البطيء كان يُتجاوَز بـ`--no-verify` فيصير مسرحياً. الحزمة الكاملة مكانها CI (`check-test-build` فحصٌ مطلوبٌ لحماية `main`).
- دخول: `admin@alroya.local` / `Admin@12345` (من `.env`). الإعدادات في `.env` (مُستثنى من git).
- **علل بيئية محسومة:** `tsx watch` معطوب على هذا الجهاز → أمر `dev` يستخدم `node --watch-path=./server ... --import tsx`. Docker Desktop ينهار بعلّة «Inference manager» → عند الحاجة: أوقف كل عمليات docker ثم أعد التشغيل. الكوكي `sameSite:"lax"` ليعمل على localhost.
- **قاعدة الاختبار المحلّية (erp-test-db@3310، MySQL 8.4.9):** `pnpm test`/`pre-commit` يفترضان مخطّطاً مُهيَّأً بالحاليّ؛ لكن `db:push` **التزايديّ** على قاعدة منجرفة يفشل على 8.4 (`ER_DROP_INDEX_FK`) فتتجمّد القاعدة وتسقط الاختبارات بـ«Unknown column» لا علاقة لها بالكود. الحلّ: **`pnpm test:db:init`** (إسقاط+إنشاء طازج ثم `db:push` + `ci-apply-extra-migrations` — نفس مسار CI) بدل الـpush التزايديّ. حرّاسه ترفض 3306 (مرآة الإنتاج) وأيّ اسم بلا «test». التفصيل والجذر (اسم FK تلقائيّ >٦٤ محرفاً كان يُفشل `db:push` الطازج على 8.4): [`docs/local-test-db.md`](docs/local-test-db.md).

## ٣.١ الحرّاس الآليّون (ratchets) — تُفشِل CI فوراً
كلٌّ سكربت `node` مستقلّ في `scripts/`، ويُجمَّعون بـ`pnpm check:guards`:

| الأمر | يمنع |
|---|---|
| `check:emoji` | أيّ إيموجي في `client/**` (البديل: أيقونة `lucide-react`) |
| `check:money-schemas` | `z.string()` عاريةً على حقلٍ ماليّ (استعمل `positiveMoneyString`) |
| `check:form-inputs` | مدخلات نماذج خارج المكوّنات الموحّدة (`MoneyInput`/`AppSelect`) |
| `check:branch` | `?? 1` — الفرع الافتراضيّ الصامت (بابُ IDOR تاريخيّ) |
| `check:migrations` | ترقيم/سجلّ الهجرات المكسور (`_journal.json`) |
| `check:orphans` | إجراءً خادمياً بلا مستدعٍ في الواجهة (بوّابة DoD §٤) |
| `check:date-boundaries` | حدود يومٍ محلّية بدل `businessDay` (UTC) |
| `check:colors` | لون حالة/مالٍ خامّاً يتجاوز التوكنز |
| `check:authz` | انحراف جرد الصلاحيات عن خطّ الأساس |
| `check:nginx-abuse` | انحراف إعداد nginx عن عقد الحماية |

- CI يشغّل هذه العشرة **عدا `check:authz`**؛ يحرسها بدلاً منه وظيفةٌ منفصلة `authz-guard` عبر `authz-guard-diff.mjs` (فرقُ قاعدة الدمج: ترفض ما **يُدخِله الـPR** من `raw-role` أو إجراءٍ بلا بوّابة وحدة، ولا تُحاسب على انحراف `main` القائم). ويشغّل CI أيضاً `check:mobile-release` و`db:verify` و`verify-digital-cards-integrity --selftest`.
- ⇒ **`pnpm check:guards` محلياً أوسع من خطوات CI الفردية** — شغّله قبل الدفع.

## ٤. القاعدة الحاكمة (Definition of Done) — لا تُكسَر
> **اكتمال أي وحدة = خلفية (API) + واجهة (شاشة) + فحص (`pnpm check`) + تحقّق (اختبار/جولة بصرية) = ١٠٠٪.**
> الخلفية وحدها بلا شاشة = **وهم اكتمال**. تُبنى كل وحدة **شريحة رأسية كاملة** (زر في الواجهة → خدمة → معاملة ذرية → DB → عرض)، تُتحقَّق بصرياً، وتظهر في **قائمة التنقّل** — ثم ننتقل للتالية. الترتيب: الكاشير أولاً، ثم وحدة-وحدة.

**⛔ بوّابة بدء الجلسة الإلزامية (قبل أي كتابة كود — لا استثناء):** «نفّذ فوراً» أدناه **مشروطٌ** باجتياز هذه البوّابة أولاً. قبل اختيار/بدء أي وحدة:
> ١) **افحص الجلسات المتزامنة:** اسرد جلسات المشروع النشطة (`mcp__ccd_session_mgmt__list_sessions`) + `pnpm coord:list` (الصورة الحيّة للشرائح المُدّعاة)؛ أي جلسة `isRunning` في نفس المجلد = مالكة محتملة — لا تلمس شريحتها.
> ٢) **افحص العمل غير الملتزم:** `git status`؛ أي ملف معدّل/غير متعقَّب = شريحة جلسة أخرى جارية. **ممنوع** البدء بوحدة تتقاطع معه (افحص بدلالة `git diff` لا التخمين).
> ٣) **اعزل جلستك ثم ادّعِ شريحتك:** `pnpm session:new <موضوع>` (ينشئ فرع `session/<موضوع>` + قاعدة/منفذ/‎.env مستقلّين + يسجّلك)، ثم `pnpm coord:claim <موضوع> --files <ملفاتك>` **قبل أي كتابة**. الكتابة على `main` محظورة آلياً بـ hook. إن وجدت WIP لغيرك ⇒ **توقّف وأبلغ** — لا تطمس عمله.
> ٤) عند اكتشاف تقاطع لحظي أثناء العمل (ملف «تغيّر منذ القراءة»، أو ظهور ملفات/تجهيز فهرس لم تصنعه): **توقّف، لا تطمس، أبلغ المالك** — التزامن قرار تنسيقٍ للمالك لا قرارٌ تقنيٌّ تتخذه وحدك.
>
> **سبب القاعدة (٦ يونيو ٢٠٢٦):** ٣ جلسات تسابقت على `main` على شريحة المشتريات نفسها فتضاربت وتكرّر العمل. الفحص المسبق + العزل يمنعان ذلك حتماً.
>
> **🔒 الإنفاذ الآلي (coord) — لم يعد البروتوكول طوعياً:** `scripts/coord-core.mjs` + hooks في `.claude/settings.json` يُنفّذان البوّابة ميكانيكياً: **قفل ذرّي للشريحة** (`O_EXCL` ⇒ سباق مستحيل)، و**رفض الكتابة** على `main`، أو على ملف تملكه جلسة أخرى حيّة، أو على ملف ساخن لغير قائد الدمج (`_integration`). أوامر: `pnpm coord:list|status|claim|release|reclaim`. مخرج طوارئ المالك: `COORD_BYPASS=1`. التفصيل في `docs/playbook.md`.

**أسلوب العمل (تفضيل دائم — مُلزِم):** تصرّف باستقلالية كموظف محترف حريص وأمين ودقيق: **بعد اجتياز بوّابة بدء الجلسة، اختر الخطوة التالية الأصحّ ونفّذها فوراً** بالمنهجية المُحكَمة (بناء → فحص → تحقّق → مراجعة)، وأبلغ بالنتائج لا بالخيارات. **لا تعرض قوائم خيارات ولا تسأل «كيف/أيّهما نبدأ»** إلا عند قرار عملٍ مصيري غير قابل للتراجع (سياسة مالية أو حذف بيانات). الأولوية تتبع §٦ (الحالة الحيّة والقيود السارية) ثم دفاتر العمل المفتوحة في §٨.

**دورة كل جلسة (فريق وكلاء ذكي تحكمه القاعدة الحاكمة):** اختر مهمة → أنجزها **١٠٠٪ بكامل تفاصيلها** (خلفية + واجهة + شاشات + كل التحكمات والوظائف الإنتاجية والعملية، بأمان واعتمادية واستقرار) → **تحقّق + فحص + اصطياد الأخطاء وإصلاحها + متابعة** حتى التكامل الكامل → **commit + تقرير موجز ثم إنهاء الجلسة**. استعمل تنظيم الوكلاء (عمّال/مُحقِّقون/مكتشفو نواقص عبر Workflow أو مهارة `review-module`) **عند الحاجة** فقط (§٧) موازنةً مع الميزانية.

## ٥. اتفاقيات الكود (مُلزمة — يحرسها ٤٦٣ ملف اختبار + حرّاس §٣.١)
- **الذرّية:** كل عملية أعمال داخل `withTx(tx => …)` (server/services/tx.ts)؛ أي throw ⇒ ROLLBACK كامل.
- **المخزون:** بالوحدة الأساس (int) لكل (variant × branch) في `branchStock`. كل تغيير عبر `inventoryService.applyMovement` (يكتب حركة + يحدّث الرصيد تحت قفل `.for("update")`). `baseQuantity = quantity × conversionFactor` ويجب أن يكون صحيحاً.
- **الأموال:** كلها عبر `decimal.js` + `money.ts` (round2 HALF_UP، toDbMoney نصاً). **ممنوع** `parseFloat`/`Number` على الأموال.
- **⭐ المبدأ المالي الحاكم (نصّ المالك ٦/٨/٢٦): «لا دينار يضيع بصمت أو يُهدر أو يختفي أو ليس له مسار أو تبويب.»** كل مبلغٍ يدخل أو يخرج يجب أن يملك خمسةً معاً: (١) **إيصالاً** بمصدره (`receipts` بـdirection وcashBucket صحيحين)، (٢) **قيدَ دفترٍ مصنَّفاً** (entryType يضعه في تقريره الصحيح — أمانةٌ لا تُسجَّل إيراداً ولا العكس)، (٣) **أثراً في تسوية الدرج** إن مسّ النقد (computeExpectedCash/Z)، (٤) **طرفاً منسوباً إليه** (عميل/مورّد/مندوب/موظف)، (٥) **تقريراً يُظهره** يربطه بمستنده وفاعله. ومالٌ محتجَز (عربون/أمانة/عهدة) يلزمه **مسار خروجٍ ممكنٌ دائماً**: الثابت العمليّ `Σ(قيود الاحتجاز لكل مستند) = 0 ⇔ أُبرِئت`. أيّ شريحةٍ جديدة تُدقَّق على هذه الخمسة قبل الدمج — راجع ذاكرة [[money-trail-delivery-2026-08-06]] (٢٢ ثغرةً أُغلقت بهذا الميزان).
- **الدفتر (مبسّط، تلقائي):** قيد عند كل بيع/شراء/دفع/مرتجع. revenue=صافي قبل الضريبة، amount=الإجمالي، RETURN بقيم سالبة. الذمم: `customers.currentBalance` (AR موجب=مدين)، `suppliers.currentBalance` (AP).
- **المنتج:** `products` → `productVariants`(لون/قياس/تكلفة) → `productUnits`(قطعة/درزن/كرتون + معامل + باركود مستقل) → `productPrices`(سعر صريح لكل وحدة×فئة RETAIL/WHOLESALE/GOVERNMENT).
- **idempotency البيع:** `clientRequestId` → `invoices.sourceId` + قيد فريد `(sourceType,sourceId)` + إعادة محاولة الراوتر على `ER_DUP_ENTRY`.
- **الطباعة:** `client/src/lib/printing/` — إيصال POS **بالتصميم المُعلَّم** عبر `printReceipt` (راسم نقطي `receiptRaster.ts` + قالب `printBrowserReceipt` كبديل متصفح — تصميم واحد على كل النواقل) بأولوية: جسر الخادم ← WebUSB ← المتصفح. Z-report/رصيد افتتاحي عبر `printDoc` العام. **⚠️ WebUSB على Windows يتطلب استبدال تعريف الطابعة بـWinUSB (أداة Zadig) وإلا `open(): Access denied`** — طُبّق على Epson TM-T20III بجهاز الكاشير (١٠/٦) وتحقّقت الطباعة العربية فعلياً.
- **بلا إيموجي في الواجهة:** حارس `scripts/check-no-emoji-ui.mjs` (يُفعَّل في CI عبر `pnpm check:emoji`) يمنع أي إيموجي في `client/**` — استبدلها بأيقونة من `lucide-react` (نمط `<Icon aria-hidden className="size-3" />`). أي إيموجي جديد يكسر CI فوراً على main. حالة سابقة: إيموجي `🔗` في [Vouchers.tsx:473](client/src/pages/Vouchers.tsx#L473) من ead9509 كسر CI على main (٥/٧)، عولج بأيقونة `Link2` (PR #134).

## ٦. الحالة الحالية (١٦ أغسطس ٢٠٢٦ — كل رقمٍ هنا مقيسٌ على `1f861c97`)

> **سجلّ الحملات المنتهية** (كل ما كان في هذا القسم: الأمانة، الأوفلاين، العمولات، واتساب، الافتتاح التدريجي، SOD، الأداء… ) نُقل **كما هو بلا حذف** إلى [`docs/campaign-log.md`](docs/campaign-log.md) — يُقرأ عند الحاجة لا في كل جلسة. هنا **الحالة الحيّة والقيود السارية** وحدها.

**🚀 الإنتاج:** `https://srv1548487.hstgr.cloud` على Hostinger VPS **مشترك** (⛔ سراج وأودو خطّ أحمر — لا يُلمسان؛ لا reboot/ufw/توقيت بلا موافقة المالك). MySQL حاوية على `127.0.0.1:3307` محجوبة عن الإنترنت + PM2 تحت مستخدم `deploy` (`pm2-deploy.service`، الويب cluster) + nginx/TLS/HSTS + نسخ ليليّ مشفَّر gpg + سحب يوميّ لجهاز المتجر (`backup:pull-vps`). **التحديث بأمر واحد: `pnpm prod:deploy`** — سكربت ذرّيّ (`git pull --ff-only` → `install --frozen-lockfile` → `db:backup` → `db:migrate:safe` → `db:verify` → `build` → `pm2 reload`) يتوقّف عند أوّل فشل بلا مسّ الخادم القديم. التفصيل: [`docs/deployment-vps.md`](docs/deployment-vps.md).

**🗃️ الهجرات:** ملفات SQL مولَّدة (`pnpm db:generate` محلياً) تُطبَّق عبر drizzle-orm migrator — آخرها **`0184_shift_funding_source_links.sql`** (١٩١ ملفاً، ١٩٠ جدولاً في `drizzle/schema.ts`). ⛔ لا `db:push` عارياً على الإنتاج (حارس `db-push-guard.mjs`)، ولا `drizzle-kit migrate` (يفشل صامتاً).

**📱 تطبيق أندرويد أصيل «سوبر العربية»** (`android-native/`، Kotlin/Compose، مصادقة تشفيريّة بمفتاح جهاز EC P-256): على Google Play بمسار **Internal testing** فقط — **قرار المالك: لا Production ولا مراجعة Google** (الحساب شخصيّ). كل تحديثٍ للمختبِرين يلزمه رفع `versionCode` في **كل مواضعه المتزامنة** (`android-native/app/build.gradle.kts` بشقَّيه + `scripts/verify-mobile-release-env.mjs` + `.github/workflows/android-release.yml` — يحرسها `pnpm check:mobile-release`) ثم بناء AAB موقَّع عبر `android-release.yml` **ورفعٌ يدويّ** (CI لا يوصِل إلى Play).

**⚙️ وحدات مبنيّة كاملةً لكنها معطَّلة/مشروطة افتراضياً — لا تفترض أنها تعمل، ولا تحذفها ظنّاً أنها ميتة:**

| الوحدة | البوّابة الفعليّة |
|---|---|
| تعدّد الشركات (multi-tenancy) | `CONTROL_DATABASE_URL` — غيابها = المسار الأحاديّ، صفر أثر سلوكيّ |
| وضع الافتتاح (بيع الصنف غير المُفتتَح بالسالب) | `openingModeSettings` معطَّل افتراضياً + نافذة ≤٦٠ يوماً + نقديّ كامل فقط |
| البيع النقديّ دون اتصال | مفتاح **لكل جهاز**، معطَّل افتراضياً (القراءة/الكتالوج المحليّ يعملان دائماً) |
| مركز واتساب (Cloud API + المهام) | مدموج (هجرات 0106-0110)؛ صفر أثر بلا تكامل `ACTIVE`، وكل التدفّقات OFF |
| الدفتر المزدوج (P2) | الخرائط محجوبة؛ `postEntry` منفذ الكتابة الوحيد |
| مزامنة أجهزة الحضور | معطَّلة |

**⚖️ قرارات المالك السارية (لا تُنقَض بلا طلبٍ صريح):**
- **الضريبة** 0% غير شامل · **التكلفة** المتوسّط المرجّح (WAVG) · **الدفع الزائد** مسموح.
- **سقف الائتمان مُفعَّل** (`server/lib/credit.ts`): `null` = بلا حدّ، و`"0"` = نقديّ فقط — **وهو الافتراضي**؛ الوضع الصريح محصورٌ بالمدير.
- **الشحن/الكمرك** مصروفُ شركةٍ يُعترَف به **لحظة الاستلام**؛ لا يمسّ ذمّة المورّد ولا تكلفة الصنف (تكلفة الصنف = سعر المورّد وحده)، بلا تصحيح رجعيّ.
- **التوصيل المجّاني إفصاحٌ لا محاسبة:** `deliveryFree`/`deliveryWaivedAmount` لا يزيدان `total` ولا `revenue` ولا يُنشئان قيداً.
- **الهدايا:** تكلفة السطر المُهدى في قيد `GIFT_OUT` وحده (لا في `SALE`)، وخارج وعاء العمولة.
- **بضاعة الأمانة:** حصّة المودِع في `costPrice`، «طريقة الإجمالي»، العمولة على **الهامش** فقط، والتلف/الضياع على المكتبة.
- **فصل المهام (SOD):** مُنشئ ≠ مُعتمِد على الصرف وتسوية المخزون ونهاية الخدمة (admin مُستثنى للتصحيح الإداريّ).
- **عزل مدير الفرع:** لا عبور بين الفروع — `canCrossBranches` = admin/isOwner فقط (`server/lib/branchAuthority.ts`).
- لا تعامل بالصكوك · المطبعة **ديجيتال** لا أوفست · إلغاء فاتورة البيع بمسارٍ موثَّق (عكسٌ كامل) · الدفعة الأولى لخطة القسط بلا سند (حارسٌ موحَّد: خادم + ويب + أندرويد).

**🧭 اتجاه العمل الأخير (من `git log`، ١٣-١٦/٨):** حوكمة الورديات والدرج النقديّ (#596-#599: تمويلٌ إضافيّ محكوم + معالجة الدرج السالب الموروث + إنفاذ سلامة النقد والمخزون) · إعلانات الموظفين خلفيةً وواجهةً أصيلة (#589-#594) · دورة اعتماد المصروفات (0182) · دفعٌ خارجيّ مؤكَّد في POS (0183) · تصحيح الفواتير والتوصيل (#573-#584).

**⚠️ ملاحظة صحّة:** أيّ ادّعاء «مكتمل/منشور» في `docs/campaign-log.md` مؤرَّخٌ بلحظة كتابته — **تحقّق من الشيفرة قبل البناء عليه**، فقد تجاوزته حملاتٌ لاحقة.

## ٧. تنسيق الوكلاء والجلسات (منع التعارض + ذكاء الأعمال)
**نظام الوكلاء (عبر أداة Workflow):**
- **عمّال (workers):** يكتبون الكود. التوازي على ملفات مختلفة فقط، وبعزل **worktree** (`isolation: "worktree"`). **كاتب واحد لكل ملف/وحدة**.
- **مُحقِّقون (verifiers):** مراجعة عدائية متعددة العدسات (صحة/أموال/ذرّية/أمان/UX)، **للقراءة فقط** → يحكمون على كل ملاحظة (تأكيد/رفض). لا يعدّلون ⇒ لا تعارض.
- **مكتشفو النواقص (gap-finders):** ناقد اكتمال يسأل «ما الناقص؟ شاشة؟ تدفّق؟ ادعاء غير مُتحقَّق؟» → يغذّي الجولة التالية.
- النمط القياسي لكل وحدة: تصميم (لوحة خبراء) → تنفيذ (عامل) → تحقّق (عدائي) → كشف نواقص → إصلاح. أمثلة مُطبَّقة: ورشة تصميم العمود الفقري، ومراجعته العدائية التي كشفت ٧ أخطاء.

**🤝 بروتوكول الفريق (التوازي بلا تعارض = عزل + تنسيق + دمج — الطبقات الثلاث):**
> العزل وحده لا يكفي؛ التوازي الآمن يحتاج **قائداً واحداً ينسّق**، لا عدّة جلسات متكافئة تتسابق.
- **١) قائد واحد (orchestrator):** جهةٌ واحدة (جلسة قائدة أو سكربت Workflow) تُحلّل الشريحة، **تعرّف العقود** (شكل الواجهة بين الـAPI والشاشة)، توزّع **وحدات غير متقاطعة**، ثم تدمج. لا وكيل يختار لنفسه عملاً يتقاطع مع غيره.
- **٢) العقد قبل التوازي:** القائد يثبّت شكل المدخلات/المخرجات بين الطبقات **قبل** إطلاق العمّال ⇒ تبني الشاشة على عقدٍ متّفقٍ عليه فيتكامل مع الـAPI بلا انجراف.
- **٣) ملكية ملفات صريحة:** كل وحدة تُعلن ملفاتها — **كاتب واحد لكل ملف**. وحدتان لا تكتبان نفس الملف أبداً (لا حتى في worktrees منفصلة، وإلّا فتعارض دمج).
- **٤) 🔥 سجلّ الملفات الساخنة (يملكها القائد وحده، يعدّلها تسلسلياً بعد فراغ العمّال):** `server/routers.ts`، `client/src/App.tsx` (المسارات)، `client/src/components/AppLayout.tsx` (التنقّل)، `drizzle/schema.ts`، `server/seed.ts`. **الوكلاء/الشرائح لا يلمسونها** — هي مغناطيس تعارض الدمج.
- **٥) الدمج + المزامنة مع الريموت:** شرائح صغيرة، دمج متكرّر إلى `main`؛ القائد يطبّق تعديلات الملفات الساخنة + `pnpm check` + الاختبارات + جولة بصرية قبل الإغلاق، **ثم يدفع فوراً: `git push origin main`** ليبقى الريموت (`origin` = GitHub: ahrrfy/business_management_system) مطابقاً للمحلي. القاعدة: **كل دمج إلى `main` يُتبَع بدفعٍ إلى `origin/main`** (المحلي والريموت يتقدّمان معاً دائماً). الدفع بعد التحقّق فقط — لا يُدفع عملٌ غير مدموج/غير مُختبَر. التفصيل: [[github-remote]] في الذاكرة.

**🔒 الإنفاذ الميكانيكي (نظام coord) — يحوّل البروتوكول أعلاه من نيّةٍ إلى قانون:**
- **قفل ذرّي للشريحة:** `pnpm coord:claim <شريحة> --files <أنماط>` يكتب قفلاً بـ `O_EXCL` في سجلّ مشترك خارج كل worktrees ⇒ كاتبٌ واحد يفوز، **لا سباق ممكن** حتى عند بدءٍ متزامن (يغلق ثغرة ٦/٦). الأوامر: `coord:list|status|release|reclaim|heartbeat`. السجلّ: `~/.claude/coord/<projectKey>/` (أقفال بمهلة ١٥د + استرجاع متحفّظ للمعطوب).
- **hooks تمنع فعلياً** (`.claude/settings.json`): `PreToolUse` يرفض الكتابة على `main`، أو على ملفٍ تملكه جلسة أخرى حيّة، أو على ملف ساخن لغير مالك `_integration`؛ و`SessionStart` يحقن الصورة الحيّة + تحذير main. **يفشلان مفتوحَين** (لا يعطّلان عملاً مشروعاً) ومخرج طوارئ للمالك `COORD_BYPASS=1`.
- **ورشة الشريحة:** مهارة `build-slice` + `‎.claude/workflows/build-slice.js` تشغّل النمط القياسي كاملاً (عقد→كتّاب متوازون→تحقّق عدائي عبر `review-module`→نواقص→إصلاح→دمج) مع **بوّابة DoD آلية** (لا «نجاح» بلا خلفية+واجهة+فحص+اختبار+تنقّل+جولة بصرية). التفصيل التشغيلي في `docs/playbook.md`.

**🤝 التوافق والتنسيق مع Codex (وكلاء OpenAI Codex يعملون على هذا المستودع فعلياً، بفروع `codex/*`):**
- **Codex يقرأ `AGENTS.md` لا `CLAUDE.md`.** الجذر يحمل `AGENTS.md` **مؤشّراً رقيقاً** إلى هذا الملف — ⛔ **لا تنسخ محتوى `CLAUDE.md` إليه**: النسخ تنجرف حتماً (وُجدت في ١٦/٨ نسخٌ محلّية غير متتبَّعة بتاريخ ١/٨ في ٧ أشجار، بينما الأصل تقدّم ٢٥٢ التزاماً ⇒ كان Codex يبني على حقيقةٍ مجمَّدة). **مصدر الحقيقة واحد: `CLAUDE.md`.**
- **البروتوكول واحدٌ للطرفين:** جلسات Codex تشارك في coord فعلياً وتظهر في `pnpm coord:list` بادّعاءاتها وملفاتها ⇒ قبل أيّ كتابة افحص `coord:list` + `git status`، وادّعِ شريحتك، **ولا تلمس ملفاً تملكه جلسة حيّة أياً كان محرّكها** (Claude أو Codex). القفل الذرّي لا يفرّق بينهما.
- **قبل الدمج:** كلا المحرّكين يمرّان بنفس البوّابة — `pnpm check` + `pnpm check:guards` + CI أخضر. لا مسار جانبيّ لأحدهما.
- `.agents/` و`.codex/` حالةُ أدواتٍ محلّية لكل شجرة (مُستثناة من git) — لا تُلتزَم ولا يُبنى عليها.

**عزل الجلسات (git) — إلزامي ومُفعَّل آلياً (انظر «بوّابة بدء الجلسة» §٤):**
- كل جلسة على **فرع + شجرة عمل معزولة** عبر `pnpm session:new <اسم>` (ينشئ `session/<اسم>` + مجلّداً مجاوراً بفهرس git خاص + **قاعدة `erp_<اسم>` و`erp_<اسم>_test` ومنفذاً و‎.env مستقلّين** + يسجّل الجلسة في coord) — **الكتابة على `main` محظورة بـ hook**. للعرض: `pnpm session:list`، وللإزالة بعد الدمج: `pnpm session:remove <اسم>`. الدمج إلى `main` بعد بلوغ ١٠٠٪+تحقّق فقط.
- **قبل أي كتابة:** اسرد الجلسات النشطة + اقرأ `git status`. **لا جلستان تُعدّلان نفس الوحدة، ولا جلستان تتشاركان شجرة عمل/فهرس git على نفس الوحدة.** إن وجدت WIP غير ملتزم لغيرك (أو ملفات جهّزها غيرك في الفهرس): **توقّف وأبلغ المالك** — لا تُكمل ولا تطمس ولا تُرجِع عمله.
- **عزل القاعدة تلقائي:** `session:new` يُنشئ `erp_<اسم>` (تطوير) و`erp_<اسم>_test` (اختبار) لكل جلسة ويكتبهما في `.env` ⇒ زال تصادم قاعدة `erp`/`erp_test` المشتركة. (مع Docker متوقّف: تحذير + بديل يدوي.) لكل جلسة منفذها الخاص أيضاً.
- **تغييرات المخطط:** مالك واحد على فرع واحد في كل مرة (تجنّب تعارض الهجرات).
- **تتبّع الملكية عبر سجلّ coord الذرّي** (`coord:claim`) لا بالاتفاق الطوعي — هو مصدر الحقيقة لمن يملك أي شريحة/ملفات. (نظام المهام TaskCreate/`owner` يبقى للتتبّع داخل الجلسة.)

## ٨. مراجع
- **دليل النظام التشغيلي العميق** (تمتين الذاكرة/التعارض/الوكلاء): [`docs/playbook.md`](docs/playbook.md) — يُقرأ عند الحاجة. **أبقِ هذا الملف رشيقاً.**
- **سجلّ الحملات المنتهية:** [`docs/campaign-log.md`](docs/campaign-log.md) — أرشيف §٦ و§٩ السابقين حرفياً (٨٦ك). تاريخيٌّ لا مرجعيّ: **تحقّق من الشيفرة قبل البناء على أيّ ادّعاءٍ فيه.**
- **دفاتر عملٍ مفتوحة (بنودٌ لم تُغلَق بعد):**
  - [`docs/audit-followups-2026-07-02.md`](docs/audit-followups-2026-07-02.md) — بنود F1-F7 بمربّعات شطب، تُنفَّذ في بيئة DB حيّة.
  - [`docs/financial-integrity-audit-2026-07-27.md`](docs/financial-integrity-audit-2026-07-27.md) — تدقيق ٦٨ وكيلاً من الكود وحده؛ صفر حرِج، ٨ عالية. **⚠️ حُدِّث ١٦/٨: بندا عزل RBAC المذكوران فيه أُصلِحا فعلاً** (`globalSearch/rbac.ts` صار يستشير خريطة الصلاحيات بدلالة `requireModule`؛ وحارس `shift.report` يقرأ `report.shift.branchId` — [shiftRouter.ts:477](server/routers/shiftRouter.ts#L477)). المتبقّي الجوهريّ: **حوكمة تعديل `costPrice`** (تعديلٌ يدويّ بلا قيدٍ مقابلٍ ولا حارس فترة ⇒ حركة حقوقٍ صامتة + COGS رجعيّ) والتسعير اليدويّ بلا سقف.
  - [`docs/functional-audit-2026-07-17.md`](docs/functional-audit-2026-07-17.md) — بنوده التقنية مُغلقة؛ وقرارات المالك التي كانت معلَّقة فيه **حُسِمت كلّها** (إلغاء الفاتورة #563 · عزل مدير الفرع #568 · الدفعة الأولى للقسط #560).
- **أرشيف مُغلق — لا تُعِد استقصاءه:** [`docs/_audit-archive/REVIEW-2026-06-15.md`](docs/_audit-archive/REVIEW-2026-06-15.md) (مُعالَجٌ بالكامل).
- **لا يوجد `README.md`:** هذا الملف هو المدخل الوحيد للمشروع، و`docs/` يحوي ٣٥+ وثيقة تصميمٍ وتدقيقٍ مؤرَّخة.
- **ذاكرة دائمة:** `~/.claude/projects/.../memory/` (مؤشرات). **خطط المراحل:** `~/.claude/plans/`.
- **اختبارات:** `server/services/__tests__/` (الثقل الأكبر) + `client/src/lib/**/*.test.ts` + `shared/**/*.test.ts`.
