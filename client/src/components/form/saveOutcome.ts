/**
 * نتيجةُ الحفظ المُهيكَلة — العقدُ بين `RecordForm`/`SaveBar` وأيّ عمليةٍ محكومة (م٦ ق٤).
 * المصدرُ الحقيقيّ في `shared/saveOutcome.ts` (قاموسٌ موحَّد لا محلّيّ — D6)؛ هذا الملفّ مدخلُ الواجهة إليه.
 */
export * from "@shared/saveOutcome";
