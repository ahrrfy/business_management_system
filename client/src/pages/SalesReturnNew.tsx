/**
 * SalesReturnNew — «مرتجع بيع جديد» (`/sales-returns/new`).
 *
 * ⚠️ لم تعد هذه الصفحة تحمل منطقاً خاصاً بها. كانت محرّراً كاملاً مستقلّاً (٧٤٩ سطراً) يوازي
 * `/returns` بمنطقٍ مختلف: بحثٌ آخر عن الفاتورة، وحسابُ سقوف استردادٍ **محليّ** يخالف ما يفرضه
 * الخادم، ومنتقي درجٍ ثانٍ. فكان للمرتجع الواحد سلوكان وتصميمان — وهو ما اشتكاه المالك
 * (١٧/٨/٢٦): «أريد المرتجع بمكوّن مرجعيّ واحد… مفهوم وغير قابل للخطأ».
 *
 * الآن كلا المسارين يعرضان الشاشة الموحَّدة نفسها (اختيار الفاتورة → `ReturnComposer`)، ومصدر
 * الحقيقة الوحيد للسقوف والرافدين والأدراج هو الخادم (`returns.getInvoice` ⇒ `loadRefundCaps`).
 * ⛔ لا تُعِد بناء نموذج مرتجعٍ هنا — عدّل `@/components/returns/ReturnComposer` وحده.
 */
export { default } from "./Returns";
