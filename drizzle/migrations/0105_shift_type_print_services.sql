-- 0105 (٢٣/٧/٢٦): نوع وردية «خدمات الطباعة» (PRINT_SERVICES) — درج/رصيد افتتاحي/عرابين وZ-report
-- مستقلّ عن كاشير التجزئة (RETAIL). قرار المالك: فصلٌ كامل بين كاشير التجزئة وكاشير خدمات الطباعة
-- (صلاحية + درج). قبل هذا كان قسم الطباعة يبيع نقداً عبر درج التجزئة نفسه ⇒ لا محاسبة نقدية منفصلة.
--
-- المعالجة: إلحاق القيمة في **نهاية** قائمة enum (MODIFY). في MySQL 8 إضافة قيمة إلى نهاية enum
-- عمليةٌ metadata-only سريعة (لا إعادة بناء جدول) ⇒ صفر downtime، وبلا backfill (القيم القائمة تبقى
-- RETAIL/RECEPTION كما هي). النوع يدخل في openGuard ⇒ وردية مفتوحة واحدة لكل (موظّف×فرع×نوع).
-- idempotent فعلياً: إعادة التطبيق تُعيد ضبط العمود لنفس التعريف بلا أثر (يُطبَّق أيضاً في
-- ci-apply-extra-migrations كدفاع على قواعد CI الدائمة — نظير 0068 لتوسيع enum).
ALTER TABLE `shifts` MODIFY `shiftType` enum('RETAIL','RECEPTION','PRINT_SERVICES') NOT NULL DEFAULT 'RETAIL';
