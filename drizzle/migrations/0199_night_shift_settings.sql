-- الوردية الليلية العابرة منتصف الليل (0199) — تدقيق ١٧/٨/٢٦.
--
-- كان هذا المفتاح موصوفاً في تعليقات المخطط والخدمة والموجّه والواجهة **بلا عمودٍ ولا تنفيذ**:
-- ميزةٌ موثَّقة معدومة. وبدونها تُسجَّل وردية 22:00→06:00 يومين ببصمةٍ واحدة لكلٍّ ⇒ صفر ساعات
-- وصفر أجر لليلة عملٍ كاملة، ويومان موسومان «ينقص انصراف».
--
-- **معطَّلة افتراضياً** (قرار مالك: نادرة في المطبعة) ⇒ صفر أثرٍ سلوكيّ على القائم.
-- وساعةُ الفصل ٨ افتراضاً: بصمةٌ قبلها تُغلق وردية اليوم السابق ولا تفتح يوماً جديداً.
ALTER TABLE `hrAttendanceSettings`
  ADD COLUMN `nightShiftEnabled` boolean NOT NULL DEFAULT false,
  ADD COLUMN `nightShiftCutoffHour` int NOT NULL DEFAULT 8;
