/**
 * قاموس عقود الواجهة — **مصدر الحقيقة الوحيد** لمفردات الشاشات المشتركة (فلاتر/جداول/إسناد).
 *
 * السبب (بلاغ المالك ١/٩/٢٦): «شاشات النظام لا تتبع مكوّناً موحّداً للتصميم والترتيب، فمساحة
 * الفلاتر ليست بمكوّن واحد ولا تصميمها وترتيبها… وكذلك الجداول لا تظهر مَن نفّذ ومَن قام ومَن
 * المستفيد والجهة الأخرى».
 *
 * المسح الذي أثبته (١/٩/٢٦):
 *   • ٤٥ صفحة تبني «بطاقة الفلاتر» يدوياً بـ**أكثر من ٢٠ توقيع شبكة مختلف**
 *     (`grid gap-3 sm:grid-cols-2` · `grid gap-4 lg:grid-cols-2 items-start` · `grid gap-2
 *     sm:grid-cols-2 xl:grid-cols-4` …) لمفهومٍ **واحد**.
 *   • ثلاث صياغات لنفس الزرّ: «مسح الفلاتر» · «مسح كل الفلاتر» · «مسح».
 *   • ٧ أسطح فقط من ٢٦١ تحمل إسناداً داخل الصفّ (`check-operation-attribution`).
 *
 * ⛔ لا شاشة تُعيد تعريف أيٍّ من هذه النصوص محلّياً — يحرسه `shared/uiContracts.test.ts`.
 *
 * موقع `shared/`: الخادم يستهلك تسميات الإسناد أيضاً حين يبني تصديراً أو تقريراً مطبوعاً،
 * فيبقى العمود مسمّىً بنفس الاسم على الشاشة وفي ملفّ Excel وفي الورقة المطبوعة.
 */

/** مفردات مساحة الفلاتر — نصٌّ واحد لكل معنى. */
export const FILTER_LABELS = {
  /** عنوان بطاقة الفلاتر. */
  title: "الفلاتر",
  /** زرّ التصفير — **صيغة واحدة معتمدة** (كانت ثلاثاً). */
  reset: "مسح الفلاتر",
  /** وصف عدد الفلاتر المفعّلة لقارئ الشاشة. */
  activeCountLabel: "فلاتر مفعّلة",
  /** نصّ يشرح أنّ الفلاتر تنعكس على الرابط (تُشارَك وتنجو من الرجوع). */
  urlSyncHint: "الفلاتر محفوظة في الرابط — يمكن مشاركته أو الرجوع إليه.",
  /** تسمية حقل البحث الافتراضية حين لا يُمرّر المستدعي تسميةً أدقّ. */
  search: "بحث",
  /** تلميح حقلٍ يقبل الماسح الضوئيّ. */
  barcodeHint: "اكتب أو امسح باركود",
} as const;

/** مفردات مساحة البيانات (الجدول). */
export const TABLE_LABELS = {
  rowCount: "عدد السجلات",
  selected: "محدَّد",
  sortAscending: "تصاعدي",
  sortDescending: "تنازلي",
  sortNone: "بلا فرز",
  actions: "إجراء",
} as const;

/**
 * ⭐ تسميات الإسناد — الجواب المباشر على «مَن نفّذ ومَن قام ومَن المستفيد والجهة الأخرى».
 *
 * أربعة أدوار **متمايزة دلالياً** لا يجوز خلطها؛ كلٌّ منها يجيب سؤالاً مختلفاً عن الصفّ:
 *   • `performedBy` — **مَن نفّذ** الحركة في النظام (المستخدم الفاعل). مسؤوليةٌ تشغيلية.
 *   • `beneficiary` — **لمن** جرت (العميل/الموظّف/المندوب الذي وقع الأثر لصالحه). وجهةُ القيمة.
 *   • `counterparty` — **الطرف الآخر** في المستند (المورّد/الجهة الخارجية/الفرع المقابل).
 *   • `approvedBy` — **مَن اعتمد** حين يفصل النظام المُنشئ عن المُعتمِد (SOD).
 *
 * خلطُ `performedBy` بـ`beneficiary` هو بالضبط ما جعل الجداول «لا تظهر المعلومات المرادة»:
 * صفٌّ يعرض اسماً واحداً لا يقول أهو الفاعل أم المستفيد.
 */
export const ATTRIBUTION_LABELS = {
  performedBy: "نفّذها",
  beneficiary: "المستفيد",
  counterparty: "الطرف الآخر",
  approvedBy: "اعتمدها",
  at: "التوقيت",
  branch: "الفرع",
  reference: "المستند",
} as const;

export type AttributionRole = keyof typeof ATTRIBUTION_LABELS;

/** كل الأدوار المسجَّلة — للاختبار النصّيّ وللحارس. */
export const ATTRIBUTION_ROLES = Object.keys(ATTRIBUTION_LABELS) as AttributionRole[];

/**
 * إيقاع الشبكة المعتمد لمساحة الفلاتر — **بديل التوقيعات العشرين**.
 *
 * الأعمدة تُطلب بالعدد المنطقيّ (١..٤) لا بسلسلة Tailwind، فيبقى القرار «كم حقلاً في الصفّ»
 * لا «أيّ نقطة انقطاع». الفجوة ثابتة عبر النظام (`gap-3` = ١٢px، إيقاع ٤px من §Layout).
 */
export const FILTER_GRID_COLUMNS = [1, 2, 3, 4] as const;
export type FilterGridColumns = (typeof FILTER_GRID_COLUMNS)[number];

/**
 * أصناف الشبكة لكل عدد أعمدة — تُقرأ من هنا وحدها.
 * mobile-first: عمودٌ واحد على الجوّال دائماً (§Layout `mobile-first`, `horizontal-scroll`).
 */
export const FILTER_GRID_CLASS: Record<FilterGridColumns, string> = {
  1: "grid grid-cols-1 gap-3",
  2: "grid grid-cols-1 gap-3 sm:grid-cols-2",
  3: "grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3",
  4: "grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4",
};
