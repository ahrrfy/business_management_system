/**
 * البحث الشامل الذرّي عبر كل وحدات النظام — نقطة الدخول العامة.
 *
 * نقطة دخول موحَّدة تستقبل استعلاماً نصيّاً (نص/باركود/رقم وثيقة/هاتف)
 * وتُرجع نتائج مُجمَّعة بالنوع، مع توجيه دقيق لمكانها في النظام.
 *
 * **تصنيف النمط (classifyQuery):**
 *  - BARCODE     : أرقام صرفة بطول ٨-١٤ (يطابق EAN-8/12/13/UPC-A + باركود داخلي).
 *  - DOC_NUMBER  : بادئة `INV-/QT-/PO-/WO-/SR-/PR-` أو رقم صرف قصير (≤٧ خانات).
 *  - PHONE       : يبدأ بـ`+` أو يطابق نمط هاتف (≥٧ خانات بعد التطبيع).
 *  - TEXT        : كل ما عداه — اسم/SKU/مدينة/ملاحظة.
 *
 * **RBAC (يتطابق مع trpc.ts):**
 *  - الكاشير: لا يرى الموردين، المشتريات، المصاريف (إدارة فقط) — تُحجب من النتائج.
 *  - عزل الفرع: الفواتير/عروض الأسعار/أوامر الشغل/المصاريف تُقيَّد بفرع المستخدم
 *    إن لم يكن admin/manager (يطابق نمط branchScopedProcedure).
 *  - البيانات الرئيسية (منتجات/عملاء/موردين) عابرة الفروع دائماً.
 *
 * **الأداء:** Promise.all يُشغّل كل الاستعلامات بالتوازي (مجموع زمن = أبطأ استعلام
 * لا مجموعها)، وكل استعلام محدود بـlimit ٥-٨ ⇒ مجموع نتائج ≤٦٤ صفّاً مهما كان النمط.
 *
 * أُعيد تنظيم المنطق (كان ٧٢٤ سطراً في ملف واحد) إلى وحدات متماسكة تحت server/services/globalSearch/*
 * **بلا أي تغيير سلوكي**: نفس الدوال والتواقيع. هذا الملف يعيد تصدير الواجهة العامة فقط كي تبقى
 * كل المستدعيات (globalSearchRouter.ts والاختبارات) بلا أي تعديل.
 *
 * خريطة الوحدات:
 *   types            — عقد البحث (تصنيف الاستعلام + أنواع النتائج).
 *   rbac             — من يرى أي نوع كيان.
 *   orchestrator     — globalSearch: يشغّل كل البحوث بالتوازي ويرتّب النتائج.
 *   searchHr         — الموظفون والمستخدمون.
 *   searchMasterData — المنتجات/الباركود، العملاء، الموردون.
 *   searchDocuments  — الفواتير، عروض الأسعار، أوامر الشغل/الشراء، المصاريف.
 *   helpers          — تنسيق التاريخ.
 */

export type { SearchEntityType, SearchKind, SearchResult, GlobalSearchInput } from "./globalSearch/types";
export { classifyQuery } from "./globalSearch/types";
export { canSeeType } from "./globalSearch/rbac";
export { globalSearch } from "./globalSearch/orchestrator";
