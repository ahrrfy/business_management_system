/**
 * أفعالُ التدقيق لمنظومة المرتجعات — **مصدرٌ واحد** يكتب به المنفّذون ويقرأ به الرقيب.
 *
 * ## لماذا وُجد هذا الملف
 *
 * كاشفُ الشذوذ D3-ب («معالجو الإرجاع» — تركّزُ المرتجعات على شخصٍ بعينه، وهو أحد أوضح
 * مؤشّرات الاحتيال) كان يستعلم `auditLogs` بالفعل `'return.create'` — **وهو فعلٌ لا يكتبه أيّ
 * سطرٍ في الخادم**. الأفعال الحقيقيّة `return.request` و`return.approveRequest` و…
 * ⇒ الكاشفُ يُرجع **صفر صفوفٍ أبداً** مهما تركّزت المرتجعات، على الإنتاج، بصمت.
 *
 * وأخبثُ ما فيه أنّ اختباره كان **أخضرَ**: `anomalyWatch.test.ts` يُدرج صفَّ التدقيق **بيده**
 * بالفعل `'return.create'` ثمّ يتحقّق أنّ الكاشف رآه. فالاختبارُ يُثبت أنّ الكاشف يقرأ ما
 * كتبه الاختبارُ نفسه، لا ما يكتبه النظام — وهو بالضبط نمط «حارسٌ يقرأ من مصدرٍ غير الذي
 * ينفّذ عليه». الثابتُ هنا يجعل الانحراف مستحيلاً بالبناء: الكاتبُ والقارئ يستوردان الرمز
 * نفسه، فإعادةُ تسميته تُحرّك الطرفين معاً أو لا تُصرّف.
 *
 * ## العقد
 *
 * `RETURN_EXECUTED_AUDIT_ACTION` يُكتب **عند وقوع الأثر فعلاً** لا عند طلبه: مرتجعٌ خرجت به
 * بضاعةٌ إلى الرفّ ومالٌ من الدرج. مساراته الثلاثة كلّها تكتبه، و`mode` في `newValue` يُميّز
 * أيّها وقع. أمّا الطلبُ صفريّ الأثر فله فعلُه الخاصّ ولا يُحسَب تنفيذاً.
 */

/** فعلُ التدقيق لكلّ مرتجعٍ **نُفِّذ فعلاً** — الكاتبُ والرقيبُ يستعملانه معاً. */
export const RETURN_EXECUTED_AUDIT_ACTION = "return.execute" as const;

/** مسارُ التنفيذ — يُخزَّن في `newValue.mode` كي يبقى التمييز ممكناً بلا فعلٍ ثانٍ. */
export type ReturnExecutionMode =
  /** المالك ينفّذ مرتجعه مباشرةً بلا دورة اعتماد (قرار المالك ١/٩/٢٦). */
  | "OWNER_IMMEDIATE"
  /** اعتمادُ مديرٍ مستقلٍّ لطلبِ موظّف المحطة (الطابور القديم `returnRequests`). */
  | "STATION_REQUEST_APPROVAL"
  /** اعتمادُ مديرٍ مستقلٍّ لطلب تحكّمٍ محكوم (`salesControlRequests`). */
  | "GOVERNED_APPROVAL";
