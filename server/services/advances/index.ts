/* ============================================================================
 * برميل خدمة السلف (server/services/advances/index.ts) — م٣ من برنامج v2 «السهل الممتنع»
 *
 * الأصل قبل التقسيم:
 *  - `server/services/advancesService.ts` (منح/تسوية/إلغاء + حسابات الرواتب)
 *  - `server/services/voucher/employeeAdvanceCancellation.ts` (قفل الإلغاء الآمن)
 *
 * تجميعُهما تحت مجلّدٍ واحد يوثّق حدَّ الوحدة، ويُبقي المستهلكين على استيرادٍ
 * واحد. البرميل لا يُضيف منطقاً — إعادةُ تصديرٍ محضة، والاختبارات القائمة
 * تمرّ كما هي بعد تعديل مسار الاستيراد فقط.
 * ========================================================================== */

export * from "./advancesService";
export * from "./employeeAdvanceCancellation";
