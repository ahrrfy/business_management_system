# AGENTS.md — نظام إدارة أعمال الرؤية العربية

> **مصدر الحقيقة الوحيد للمشروع هو [`CLAUDE.md`](CLAUDE.md) في هذا الجذر. اقرأه أولاً وبالكامل قبل أيّ عمل.**
>
> هذا الملف **مؤشّرٌ رقيق** لا نسخة. ⛔ **لا تنسخ محتوى `CLAUDE.md` إلى هنا** — النسخ تنجرف: في ١٦/٨/٢٦ وُجدت نسخٌ محلّية غير متتبَّعة بتاريخ ١/٨ في سبع أشجار عملٍ بينما الأصل كان قد تقدّم ٢٥٢ التزاماً، فكانت وكلاء Codex تبني على حقيقةٍ مجمَّدة. أيّ تحديثٍ للقواعد يُكتب في `CLAUDE.md` وحده.

## ما يلزمك قبل أوّل سطر كود

1. **اقرأ [`CLAUDE.md`](CLAUDE.md)** — الهوية، البنية، الأوامر، القاعدة الحاكمة (DoD)، اتفاقيات الكود المُلزِمة، الحالة الحيّة.
2. **اجتز بوّابة بدء الجلسة** (§٤ هناك): افحص الجلسات المتزامنة `pnpm coord:list` + `git status`، ثم اعزل جلستك (`pnpm session:new <موضوع>`) وادّعِ شريحتك (`pnpm coord:claim <موضوع> --files <ملفاتك>`) **قبل أيّ كتابة**.
3. **التنسيق مشتركٌ بين المحرّكات:** جلسات Codex وClaude تتشارك سجلّ coord نفسه والقفل الذرّي نفسه. لا تلمس ملفاً تملكه جلسة حيّة أياً كان محرّكها؛ وإن وجدت عملاً غير ملتزمٍ لغيرك ⇒ **توقّف وأبلغ المالك، لا تطمسه**.

## بوّابة ما قبل الدفع (واحدة للجميع)

```
pnpm check          # فحص الأنواع
pnpm check:guards   # الحرّاس العشرة (§٣.١ في CLAUDE.md)
```

الحزمة الكاملة `pnpm test` (TZ=UTC) مطلوبةٌ لأيّ شريحةٍ ماليّة، وCI أخضر شرطُ الدمج إلى `main`.

## ملفات أدوات محلّية

`.agents/` و`.codex/` حالةُ أدواتٍ خاصّة بكل شجرة عمل ومُستثناة من git — لا تُلتزَم ولا يُبنى عليها.
